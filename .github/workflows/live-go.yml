name: Live Go (manual)

# Live Go contract suite against a hosted backend.
# Offline golden-fixture replay runs in the main CI `go-binding` job via
# `go test -race ./...`. This workflow is dispatch-only until a hosted contract
# env exists (see rust-migration-map open handoff items). Mirrors
# `.github/workflows/live-rust.yml`.

on:
  workflow_dispatch:
    inputs:
      base_url:
        description: 'SOLVAPAY_LIVE_BASE_URL'
        required: true
        type: string
      enable_stripe:
        description: 'Run requires:stripe scenarios'
        required: false
        type: boolean
        default: false

jobs:
  live-go:
    name: Go live contract
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdks/go
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain + WASI target
        run: rustup show && rustup target add wasm32-wasip1

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Rebuild embedded WASI guest
        run: ./sdks/go/scripts/build-wasm.sh

      - name: Run live Go contract suite
        env:
          SOLVAPAY_LIVE_BASE_URL: ${{ inputs.base_url }}
          SOLVAPAY_LIVE_API_KEY: ${{ secrets.SOLVAPAY_SHADOW_API_KEY }}
          SOLVAPAY_LIVE_ENABLE_STRIPE: ${{ inputs.enable_stripe }}
          SOLVAPAY_LIVE_OUT: ${{ github.workspace }}/contract/live/output/go-live-report.json
        run: |
          mkdir -p "${{ github.workspace }}/contract/live/output"
          go run ./cmd/live-contract

      - name: Upload go-live-report.json
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-live-report
          path: contract/live/output/go-live-report.json
