{
  "name": "@example/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end coverage for the TypeScript checkout examples, run against a live local SolvaPay platform stack",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.5.0",
    "dotenv": "^17.2.4",
    "typescript": "^5.9.3"
  }
}
