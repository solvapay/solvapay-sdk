# Rust SDK examples

| Example | Description |
| --- | --- |
| [`get-merchant`](./get-merchant/) | Async `Client::get_merchant` with local `.env` |

Shared dotenv paths live in [`env/`](./env/) (`solvapay-rust-examples-env`).
