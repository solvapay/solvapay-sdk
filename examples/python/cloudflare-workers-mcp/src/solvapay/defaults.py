"""Frozen payment-required copy for the Worker bundle.

Mirrors `_PAYMENT_REQUIRED` in the Python SDK defaults module. That file is
generated, so this copy stays hand-written: the header gate rejects a
generated marker outside the codegen registry.
"""

_PAYMENT_REQUIRED = "Payment required"
