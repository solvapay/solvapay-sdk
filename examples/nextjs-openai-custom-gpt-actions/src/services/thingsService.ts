/**
 * Re-export the shared demo services for use in this example
 * Uses the tasks terminology from @solvapay/demo-services
 */
export { 
  createTask, 
  getTask, 
  listTasks, 
  deleteTask, 
  clearAllTasks 
} from '@solvapay/demo-services';
