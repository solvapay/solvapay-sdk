{
  "name": "@example/cloudflare-workers-mcp",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers starter for SolvaPay MCP — single `createSolvaPayMcpFetch` call over the Workers runtime. Ships the same paywalled demo toolbox as `supabase-edge-mcp`; the only runtime difference is the HTTP entrypoint.",
  "scripts": {
    "build": "cross-env INPUT=mcp-app.html vite build && mkdir -p src/assets && cp dist/mcp-app.html src/assets/mcp-app.html",
    "dev": "cross-env INPUT=mcp-app.html vite build --watch",
    "predeploy": "pnpm -w build:packages && pnpm build",
    "deploy": "node scripts/deploy.mjs",
    "preflight:prod": "node scripts/preflight-prod.mjs",
    "deploy:prod": "pnpm -w build:packages && pnpm build && node scripts/deploy.mjs --prod",
    "serve:local": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260615.1",
    "@modelcontextprotocol/ext-apps": "^1.7.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@solvapay/mcp": "workspace:*",
    "@solvapay/mcp-core": "workspace:*",
    "@solvapay/react": "workspace:*",
    "@solvapay/server": "workspace:*",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "cross-env": "^10.1.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "typescript": "^5.9.2",
    "vite": "^8.0.16",
    "vite-plugin-singlefile": "^2.3.3",
    "wrangler": "^4.100.0",
    "zod": "^4.3.6"
  }
}
