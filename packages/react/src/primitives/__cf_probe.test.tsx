import { describe, it, expect } from 'vitest'

/**
 * Scratch file. Safe to delete.
 *
 * Used to isolate the render loop described in CardFields.test.tsx. Kept only
 * because this session could not delete files in this folder; it asserts
 * nothing and should be removed rather than extended.
 */
describe('scratch (delete me)', () => {
  it.skip('placeholder', () => {
    expect(true).toBe(true)
  })
})
