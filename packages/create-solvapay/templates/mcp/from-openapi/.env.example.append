
# Upstream API key the generated tool handlers send via
# `Authorization: Bearer …` or `<header-name>: …`. Only present when the
# OpenAPI spec uses bearer or apiKey auth. Scaffold populates this from
# `selections.upstreamAuth.key`.
# UPSTREAM_API_KEY=
