---
'@solvapay/mcp-core': patch
---

Single-source the four intent-tool names from `MCP_TOOL_NAMES`, and cut duplicated description text: `mode` hint moves onto the param schema, cross-reference tails and host-rendering prose drop, and trigger phrases lead each description.
