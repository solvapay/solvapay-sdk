---
'@solvapay/server': minor
'@solvapay/mcp-core': patch
'@solvapay/react': minor
---

Add a UI-only `get_history` tool that returns product charges and account-wide credit activity, plus `useHistory` so the account widget can fetch them on section mount.
