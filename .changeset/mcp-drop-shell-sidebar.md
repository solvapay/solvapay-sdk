---
'@solvapay/react': minor
---

Drop the MCP shell sidebar. `McpSellerDetailsCard`, `McpCustomerDetailsCard`, and `hideDetailCards` are gone. Identity is `McpPayingAs` inside the payment form (`Paying as {email}`), not a shell provenance line.
