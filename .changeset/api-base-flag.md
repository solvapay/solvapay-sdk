---
'@solvapay/init': minor
'solvapay': minor
'create-solvapay': minor
---

Add `--api-base <url>` so a scaffold can keep checkout path deps (`--dev`) while pointing auth, product lookup, and `.env` at a local API origin.
