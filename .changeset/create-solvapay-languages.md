---
'create-solvapay': minor
'@solvapay/init': minor
'solvapay': minor
---

Add a `--language` dimension (`ts`, `python`, `ruby`, `go`, `rust`) to `create-solvapay` and make `solvapay init` detect the project language from manifests instead of always creating a Node `package.json`.
