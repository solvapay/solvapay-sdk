---
'@solvapay/core': patch
'@solvapay/server-wasm': minor
---

Install the public-safe browser WASM into the MCP App widget from inlined bytes, and expose `countsUsage`, `meterName`, and `usageRate` on the browser profile so account and checkout can resolve after init.
