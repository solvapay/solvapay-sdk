---
'@solvapay/server': minor
---

Add `isManagedMcp` as the canonical Managed MCP product flag on SDK product responses and create/list inputs. Deprecated `isMcpPay` remains accepted and emitted for wire compatibility.
