---
'@solvapay/release-train': minor
---

<!-- @generated by pnpm gen — do not edit -->

Core surface: added symbol `BALANCE_RECONCILE_DELAYS_MS`, added symbol `BUSINESS_COUNTRY_DISPLAY_NAMES`, added symbol `BUSINESS_COUNTRY_OPTIONS`.

- added symbol `BALANCE_RECONCILE_DELAYS_MS`
- added symbol `BUSINESS_COUNTRY_DISPLAY_NAMES`
- added symbol `BUSINESS_COUNTRY_OPTIONS`
- added symbol `COUNTRY_TO_TAX_ID_TYPE`
- added symbol `MCP_TOOL_NAMES`
- added symbol `POSTAL_CODE_REQUIRED_COUNTRIES`
- added symbol `REVERSE_CHARGE_NOTE`
- added symbol `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE`
- added symbol `STATE_REQUIRED_COUNTRIES`
- added symbol `SUPPORTED_BUSINESS_COUNTRIES`
- added symbol `TAX_BEHAVIORS`
- added symbol `TAX_EXCLUSIVE_CURRENCIES`
- added symbol `TAX_ID_EXAMPLE_BY_COUNTRY`
- added symbol `TAX_ID_TYPES`
- added symbol `TAX_NOT_COLLECTED_NOTE`
- added symbol `TOPUP_BALANCE_POLL_DELAYS_MS`
- added symbol `activatePlan`
- added symbol `appendPaidToolDescription`
- added symbol `assertResponseResult`
- added symbol `assertValidProductRef`
- added symbol `assignCredits`
- added symbol `attachBusinessDetails`
- added symbol `attachBusinessDetailsValidationError`
- added symbol `billingCycle`
- added symbol `bootstrapMcpProduct`
- added symbol `buildCreateCustomerParams`
- added symbol `buildCustomerSnapshot`
- added symbol `buildGateMessage`
- added symbol `buildNudgeMessage`
- added symbol `buildPayableToolResult`
- added symbol `buildPaywallGate`
- added symbol `buildPromptDescriptorMetadata`
- added symbol `buildPromptUserMessage`
- added symbol `buildToolDescriptorMetadata`
- added symbol `cancelPurchase`
- added symbol `charges`
- added symbol `checkLimits`
- added symbol `classifyCancelError`
- added symbol `classifyCreateError`
- added symbol `classifyCustomerRef`
- added symbol `classifyLookupError`
- added symbol `classifyPaywallState`
- added symbol `classifyReactivateError`
- added symbol `cloneProduct`
- added symbol `coerceCustomerOptions`
- added symbol `compileStringFieldInputSchemaJson`
- added symbol `configureMcpPlans`
- added symbol `countsUsage`
- added symbol `createCheckoutSession`
- added symbol `createCustomer`
- added symbol `createCustomerSession`
- added symbol `createPaymentIntent`
- added symbol `createPlan`
- added symbol `createProduct`
- added symbol `createTopupPaymentIntent`
- added symbol `creditSignals`
- added symbol `creditsPerUnitFromBalance`
- added symbol `creditsToDisplayMinorUnits`
- added symbol `customerRefFromClaims`
- added symbol `decidePaywallOutcome`
- added symbol `decodeJwtPayloadUnverified`
- added symbol `defaultMcpBearerExpectations`
- added symbol `deletePlan`
- added symbol `deleteProduct`
- added symbol `deriveActiveProducts`
- added symbol `deriveDefaultView`
- added symbol `deriveIcons`
- added symbol `deriveTaxIdType`
- added symbol `disableAutoRecharge`
- added symbol `ensureCustomerNext`
- added symbol `ensureOutputSchemaObjectType`
- added symbol `evaluateBalanceObservation`
- added symbol `evaluateCachedLimits`
- added symbol `evaluateClaimedLimits`
- added symbol `evaluateFreshLimits`
- added symbol `evaluateProductReadiness`
- added symbol `extractBackendCustomerRef`
- added symbol `extractBearerToken`
- added symbol `fetchJwks`
- added symbol `formatCompactCredits`
- added symbol `formatPrice`
- added symbol `formatSubtotalLabel`
- added symbol `formatVatSummaryLabel`
- added symbol `freeLimitsAgree`
- added symbol `freeMeterNamePattern`
- added symbol `freeToolDescriptionSuffix`
- added symbol `gateNext`
- added symbol `getAutoRecharge`
- added symbol `getBusinessCountryOptions`
- added symbol `getCreditActivity`
- added symbol `getCustomer`
- added symbol `getCustomerAddressFieldErrors`
- added symbol `getCustomerBalance`
- added symbol `getHistoryNext`
- added symbol `getMerchant`
- added symbol `getPaymentMethod`
- added symbol `getPlatformConfig`
- added symbol `getPostalCodeFieldLabel`
- added symbol `getPostalCodePlaceholder`
- added symbol `getProduct`
- added symbol `getSellerTaxIdentifierDisplayLabel`
- added symbol `getStateFieldLabel`
- added symbol `getTaxIdExample`
- added symbol `getTaxIdFieldLabel`
- added symbol `getTaxIdHelperText`
- added symbol `getUserInfo`
- added symbol `headlineCharges`
- added symbol `historyRows`
- added symbol `includedUnits`
- added symbol `invokePayableNext`
- added symbol `isCachedCustomerRefValid`
- added symbol `isCustomerAddressComplete`
- added symbol `isEmailConflict`
- added symbol `isErrorResult`
- added symbol `isPostalCodeRequired`
- added symbol `isStateRequired`
- added symbol `isTaxIdType`
- added symbol `isUnlimitedRemaining`
- added symbol `isZeroDecimalCurrency`
- added symbol `linkLabel`
- added symbol `listPlans`
- added symbol `listProducts`
- added symbol `listPurchases`
- added symbol `makeResponseResult`
- added symbol `mapRouteError`
- added symbol `mcpBootstrap`
- added symbol `mcpCallBuiltinTool`
- added symbol `mcpDispatch`
- added symbol `mcpOauthRequest`
- added symbol `mcpReadResource`
- added symbol `mcpResolveAuth`
- added symbol `mcpViewMaps`
- added symbol `meterName`
- added symbol `minorUnitsPerMajor`
- added symbol `nextActionFor`
- added symbol `normalizeCancelResponse`
- added symbol `normalizeFreeLimit`
- added symbol `normalizeReactivateResponse`
- added symbol `overlayClaimedLimits`
- added symbol `paywallErrorToClientPayload`
- added symbol `paywallStructuredContentSchema`
- added symbol `paywallToolResult`
- added symbol `peggedCreditsPerUnit`
- added symbol `perUnitCharge`
- added symbol `planConsequence`
- added symbol `planLadder`
- added symbol `planPricingShape`
- added symbol `processPaymentIntent`
- added symbol `projectPaymentIntentResult`
- added symbol `projectTopupProcessOutcome`
- added symbol `projectUsageSnapshot`
- added symbol `reactivatePurchase`
- added symbol `requireProductRef`
- added symbol `resolveAccountState`
- added symbol `resolveAuthenticatedUser`
- added symbol `resolveBuyerCountry`
- added symbol `resolveCheckLimitsParams`
- added symbol `resolveCustomerRef`
- added symbol `resolveDisplayMode`
- added symbol `resolveFallbackGateLimits`
- added symbol `resolvePlanShape`
- added symbol `resolveProductRef`
- added symbol `resolvePurchaseCustomerRef`
- added symbol `resolveReturnUrl`
- added symbol `resolveSellerIdentityDisplay`
- added symbol `resolveTaxBehavior`
- added symbol `resolveTaxTreatmentNote`
- added symbol `resolveUsageExtra`
- added symbol `retryNextDelayMs`
- added symbol `saveAutoRecharge`
- added symbol `selectActivePlanPurchase`
- added symbol `selectActivePurchases`
- added symbol `shouldRetryUsageError`
- added symbol `shouldShowTaxRow`
- added symbol `tierBands`
- added symbol `tierMeters`
- added symbol `toMajorUnits`
- added symbol `topupProcessNext`
- added symbol `trackUsage`
- added symbol `trackUsageBulk`
- added symbol `trialDays`
- added symbol `updateCustomer`
- added symbol `updatePlan`
- added symbol `updateProduct`
- added symbol `usageRate`
- added symbol `validateActivatePlanParams`
- added symbol `validateAttachBusinessDetailsParams`
- added symbol `validateBusinessDetails`
- added symbol `validateCheckoutSessionParams`
- added symbol `validateCreatePaymentIntentParams`
- added symbol `validateGetProductParams`
- added symbol `validateListPlansParams`
- added symbol `validateProcessPaymentIntentParams`
- added symbol `validatePublicBaseUrl`
- added symbol `validatePurchaseRef`
- added symbol `validateTopupPaymentIntentParams`
- added symbol `verifyWebhook`
- added boundary type `ActiveProduct`
- added boundary type `AllowConsequence`
- added boundary type `AuthResolutionInput`
- added boundary type `AuthenticatedUser`
- added boundary type `BillingCycle`
- added boundary type `BillingInterval`
- added boundary type `BuildPromptDescriptorMetadataOptions`
- added boundary type `BuildToolDescriptorMetadataOptions`
- added boundary type `BusinessCountryOption`
- added boundary type `BusinessDetails`
- added boundary type `BusinessDetailsInput`
- added boundary type `BusinessDetailsValidationError`
- added boundary type `BusinessDetailsValidationIssue`
- added boundary type `CachedLimitsEvaluation`
- added boundary type `Charge`
- added boundary type `ChargePer`
- added boundary type `CheckLimitsParams`
- added boundary type `CoercedCustomerOptions`
- added boundary type `CreateCustomerParams`
- added boundary type `CreateErrorKind`
- added boundary type `CreditSignals`
- added boundary type `CreditsToDisplayInput`
- added boundary type `CustomerRefKind`
- added boundary type `CustomerSnapshot`
- added boundary type `DefaultMcpBearerExpectations`
- added boundary type `EnsureCustomerAction`
- added boundary type `EnsureCustomerCacheWrite`
- added boundary type `EnsureCustomerNextOutput`
- added boundary type `EnsureCustomerState`
- added boundary type `EnsurePending`
- added boundary type `FreeLimit`
- added boundary type `FreeLimitInput`
- added boundary type `FreeLimitScope`
- added boundary type `FreshLimitsEvaluation`
- added boundary type `GateAction`
- added boundary type `GateCacheOp`
- added boundary type `GateContent`
- added boundary type `GateDriverState`
- added boundary type `GateNextOutput`
- added boundary type `GetHistoryAction`
- added boundary type `GetHistoryNextOutput`
- added boundary type `GetHistoryPending`
- added boundary type `GetHistoryState`
- added boundary type `HelperErrorResult`
- added boundary type `HistoryChargeRow`
- added boundary type `HistoryCreditRow`
- added boundary type `HistoryRows`
- added boundary type `IncludedUsage`
- added boundary type `InvokePayableAction`
- added boundary type `InvokePayableNextOutput`
- added boundary type `InvokePayableState`
- added boundary type `InvokePayableTrack`
- added boundary type `LookupErrorKind`
- added boundary type `McpContainerDimensions`
- added boundary type `McpContentBlock`
- added boundary type `McpDisplayMode`
- added boundary type `McpDisplayModeState`
- added boundary type `McpHostedRail`
- added boundary type `McpPayableToolResult`
- added boundary type `McpPaywallToolResult`
- added boundary type `McpSafeAreaInsets`
- added boundary type `McpViewMaps`
- added boundary type `MerchantBranding`
- added boundary type `PaymentIntentProjection`
- added boundary type `PaymentIntentSource`
- added boundary type `PaywallAutoRecharge`
- added boundary type `PaywallBalance`
- added boundary type `PaywallClientPayload`
- added boundary type `PaywallGate`
- added boundary type `PaywallGateKind`
- added boundary type `PaywallGateLimits`
- added boundary type `PaywallLimits`
- added boundary type `PaywallNextAction`
- added boundary type `PaywallOutcome`
- added boundary type `PaywallPlanSummary`
- added boundary type `PaywallRecoveryLinks`
- added boundary type `PaywallState`
- added boundary type `PlanPricingShape`
- added boundary type `PricingShape`
- added boundary type `ProductReadinessInput`
- added boundary type `ProductReadinessPlan`
- added boundary type `ProductReadinessResult`
- added boundary type `PromptContent`
- added boundary type `PromptDescriptorMetadata`
- added boundary type `PromptMessage`
- added boundary type `PromptUserMessage`
- added boundary type `ResponseEnvelope`
- added boundary type `RouteErrorInput`
- added boundary type `RouteErrorKind`
- added boundary type `SdkError`
- added boundary type `SellerIdentityDisplay`
- added boundary type `SellerIdentityInput`
- added boundary type `SellerIdentityRow`
- added boundary type `TaxIdType`
- added boundary type `Tier`
- added boundary type `TierMode`
- added boundary type `ToolAnnotations`
- added boundary type `ToolDescriptorMetadata`
- added boundary type `ToolIcon`
- added boundary type `TopupPending`
- added boundary type `TopupProcessAction`
- added boundary type `TopupProcessNextOutput`
- added boundary type `TopupProcessOutcome`
- added boundary type `TopupProcessState`
- added boundary type `UsageClass`
- added boundary type `UsageExtra`
- added boundary type `UsageRate`
- added boundary type `UsageSnapshot`
- added boundary type `ValidateBusinessDetailsResult`
- added boundary type `WebhookError`
- added boundary type `WebhookErrorCode`
- added `activatePlan` (POST /v1/sdk/activate)
- added `assignCredits` (POST /v1/sdk/customers/{reference}/credits)
- added `attachBusinessDetails` (POST /v1/sdk/payment-intents/{processorPaymentId}/business-details)
- added `bootstrapMcpProduct` (POST /v1/sdk/products/mcp/bootstrap)
- added `cancelPurchase` (POST /v1/sdk/purchases/{purchaseRef}/cancel)
- added `checkLimits` (POST /v1/sdk/limits)
- added `cloneProduct` (POST /v1/sdk/products/{productRef}/clone)
- added `configureMcpPlans` (PUT /v1/sdk/products/{productRef}/mcp/plans)
- added `createCheckoutSession` (POST /v1/sdk/checkout-sessions)
- added `createCustomer` (POST /v1/sdk/customers)
- added `createCustomerSession` (POST /v1/sdk/customers/customer-sessions)
- added `createPaymentIntent` (POST /v1/sdk/payment-intents)
- added `createPlan` (POST /v1/sdk/products/{productRef}/plans)
- added `createProduct` (POST /v1/sdk/products)
- added `createTopupPaymentIntent` (POST /v1/sdk/payment-intents)
- added `deletePlan` (DELETE /v1/sdk/products/{productRef}/plans/{planRef})
- added `deleteProduct` (DELETE /v1/sdk/products/{productRef})
- added `disableAutoRecharge` (DELETE /v1/sdk/auto-recharge)
- added `fetchJwks`
- added `getAutoRecharge` (GET /v1/sdk/auto-recharge)
- added `getCreditActivity` (GET /v1/sdk/credits/activity)
- added `getCustomer` (GET /v1/sdk/customers/{reference})
- added `getCustomerBalance` (GET /v1/sdk/customers/{reference}/balance)
- added `getMerchant` (GET /v1/sdk/merchant)
- added `getPaymentMethod` (GET /v1/sdk/payment-method)
- added `getPlatformConfig` (GET /v1/sdk/platform-config)
- added `getProduct` (GET /v1/sdk/products/{productRef})
- added `getUserInfo` (POST /v1/sdk/user-info)
- added `listPlans` (GET /v1/sdk/products/{productRef}/plans)
- added `listProducts` (GET /v1/sdk/products)
- added `listPurchases` (GET /v1/sdk/purchases)
- added `mcpBootstrap`
- added `mcpCallBuiltinTool`
- added `mcpDispatch`
- added `mcpOauthRequest`
- added `mcpReadResource`
- added `mcpResolveAuth`
- added `processPaymentIntent` (POST /v1/sdk/payment-intents/{processorPaymentId}/process)
- added `reactivatePurchase` (POST /v1/sdk/purchases/{purchaseRef}/reactivate)
- added `saveAutoRecharge` (PUT /v1/sdk/auto-recharge)
- added `trackUsage` (POST /v1/sdk/usages)
- added `trackUsageBulk` (POST /v1/sdk/usages/bulk)
- added `updateCustomer` (PATCH /v1/sdk/customers/{reference})
- added `updatePlan` (PUT /v1/sdk/products/{productRef}/plans/{planRef})
- added `updateProduct` (PUT /v1/sdk/products/{productRef})
- added error code `webhook.missing_signature`
- added error code `webhook.malformed_signature`
- added error code `webhook.timestamp_too_old`
- added error code `webhook.invalid_signature`
- added error code `webhook.invalid_payload`
- added error code `paywall.payment_required`
- added error code `paywall.activation_required`
- added error code `mcp.rawHandlerReturn`
- changed frozen `defaults` block
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `go`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `node-native`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `python`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `ruby`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `rust`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `typescript`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `wasm-browser`
- exposed `BALANCE_RECONCILE_DELAYS_MS` on facade `wasm-edge`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `go`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `node-native`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `python`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `ruby`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `rust`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `typescript`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `wasm-browser`
- exposed `BUSINESS_COUNTRY_DISPLAY_NAMES` on facade `wasm-edge`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `go`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `node-native`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `python`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `ruby`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `rust`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `typescript`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `wasm-browser`
- exposed `BUSINESS_COUNTRY_OPTIONS` on facade `wasm-edge`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `go`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `node-native`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `python`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `ruby`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `rust`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `typescript`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `wasm-browser`
- exposed `COUNTRY_TO_TAX_ID_TYPE` on facade `wasm-edge`
- exposed `MCP_TOOL_NAMES` on facade `go`
- exposed `MCP_TOOL_NAMES` on facade `node-native`
- exposed `MCP_TOOL_NAMES` on facade `python`
- exposed `MCP_TOOL_NAMES` on facade `python-mcp`
- exposed `MCP_TOOL_NAMES` on facade `ruby`
- exposed `MCP_TOOL_NAMES` on facade `ruby-mcp`
- exposed `MCP_TOOL_NAMES` on facade `rust-mcp`
- exposed `MCP_TOOL_NAMES` on facade `typescript`
- exposed `MCP_TOOL_NAMES` on facade `wasm-edge`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `go`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `node-native`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `python`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `ruby`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `typescript`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `wasm-browser`
- exposed `POSTAL_CODE_REQUIRED_COUNTRIES` on facade `wasm-edge`
- exposed `REVERSE_CHARGE_NOTE` on facade `go`
- exposed `REVERSE_CHARGE_NOTE` on facade `node-native`
- exposed `REVERSE_CHARGE_NOTE` on facade `python`
- exposed `REVERSE_CHARGE_NOTE` on facade `ruby`
- exposed `REVERSE_CHARGE_NOTE` on facade `rust`
- exposed `REVERSE_CHARGE_NOTE` on facade `typescript`
- exposed `REVERSE_CHARGE_NOTE` on facade `wasm-browser`
- exposed `REVERSE_CHARGE_NOTE` on facade `wasm-edge`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `go`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `node-native`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `python`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `ruby`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `rust`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `typescript`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `wasm-browser`
- exposed `SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE` on facade `wasm-edge`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `go`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `node-native`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `python`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `ruby`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `typescript`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `wasm-browser`
- exposed `STATE_REQUIRED_COUNTRIES` on facade `wasm-edge`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `go`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `node-native`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `python`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `ruby`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `rust`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `typescript`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `wasm-browser`
- exposed `SUPPORTED_BUSINESS_COUNTRIES` on facade `wasm-edge`
- exposed `TAX_BEHAVIORS` on facade `go`
- exposed `TAX_BEHAVIORS` on facade `node-native`
- exposed `TAX_BEHAVIORS` on facade `python`
- exposed `TAX_BEHAVIORS` on facade `ruby`
- exposed `TAX_BEHAVIORS` on facade `rust`
- exposed `TAX_BEHAVIORS` on facade `typescript`
- exposed `TAX_BEHAVIORS` on facade `wasm-browser`
- exposed `TAX_BEHAVIORS` on facade `wasm-edge`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `go`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `node-native`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `python`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `ruby`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `rust`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `typescript`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `wasm-browser`
- exposed `TAX_EXCLUSIVE_CURRENCIES` on facade `wasm-edge`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `go`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `node-native`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `python`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `ruby`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `rust`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `typescript`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `wasm-browser`
- exposed `TAX_ID_EXAMPLE_BY_COUNTRY` on facade `wasm-edge`
- exposed `TAX_ID_TYPES` on facade `go`
- exposed `TAX_ID_TYPES` on facade `node-native`
- exposed `TAX_ID_TYPES` on facade `python`
- exposed `TAX_ID_TYPES` on facade `ruby`
- exposed `TAX_ID_TYPES` on facade `rust`
- exposed `TAX_ID_TYPES` on facade `typescript`
- exposed `TAX_ID_TYPES` on facade `wasm-browser`
- exposed `TAX_ID_TYPES` on facade `wasm-edge`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `go`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `node-native`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `python`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `ruby`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `rust`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `typescript`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `wasm-browser`
- exposed `TAX_NOT_COLLECTED_NOTE` on facade `wasm-edge`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `go`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `node-native`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `python`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `ruby`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `rust`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `typescript`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `wasm-browser`
- exposed `TOPUP_BALANCE_POLL_DELAYS_MS` on facade `wasm-edge`
- exposed `activatePlan` on facade `capi`
- exposed `activatePlan` on facade `go`
- exposed `activatePlan` on facade `node-native`
- exposed `activatePlan` on facade `python`
- exposed `activatePlan` on facade `python-mcp`
- exposed `activatePlan` on facade `ruby`
- exposed `activatePlan` on facade `rust`
- exposed `activatePlan` on facade `typescript`
- exposed `activatePlan` on facade `wasm-edge`
- exposed `appendPaidToolDescription` on facade `go`
- exposed `appendPaidToolDescription` on facade `node-native`
- exposed `appendPaidToolDescription` on facade `python`
- exposed `appendPaidToolDescription` on facade `ruby`
- exposed `appendPaidToolDescription` on facade `rust`
- exposed `appendPaidToolDescription` on facade `rust-mcp`
- exposed `appendPaidToolDescription` on facade `typescript`
- exposed `appendPaidToolDescription` on facade `wasm-edge`
- exposed `assertResponseResult` on facade `go`
- exposed `assertResponseResult` on facade `node-native`
- exposed `assertResponseResult` on facade `python`
- exposed `assertResponseResult` on facade `python-mcp`
- exposed `assertResponseResult` on facade `ruby`
- exposed `assertResponseResult` on facade `ruby-mcp`
- exposed `assertResponseResult` on facade `rust-mcp`
- exposed `assertResponseResult` on facade `typescript`
- exposed `assertResponseResult` on facade `wasm-edge`
- exposed `assertValidProductRef` on facade `go`
- exposed `assertValidProductRef` on facade `node-native`
- exposed `assertValidProductRef` on facade `python`
- exposed `assertValidProductRef` on facade `python-mcp`
- exposed `assertValidProductRef` on facade `ruby`
- exposed `assertValidProductRef` on facade `rust`
- exposed `assertValidProductRef` on facade `typescript`
- exposed `assertValidProductRef` on facade `wasm-edge`
- exposed `assignCredits` on facade `capi`
- exposed `assignCredits` on facade `go`
- exposed `assignCredits` on facade `node-native`
- exposed `assignCredits` on facade `python`
- exposed `assignCredits` on facade `ruby`
- exposed `assignCredits` on facade `rust`
- exposed `assignCredits` on facade `typescript`
- exposed `assignCredits` on facade `wasm-edge`
- exposed `attachBusinessDetails` on facade `capi`
- exposed `attachBusinessDetails` on facade `go`
- exposed `attachBusinessDetails` on facade `node-native`
- exposed `attachBusinessDetails` on facade `python`
- exposed `attachBusinessDetails` on facade `ruby`
- exposed `attachBusinessDetails` on facade `rust`
- exposed `attachBusinessDetails` on facade `typescript`
- exposed `attachBusinessDetails` on facade `wasm-edge`
- exposed `attachBusinessDetailsValidationError` on facade `go`
- exposed `attachBusinessDetailsValidationError` on facade `node-native`
- exposed `attachBusinessDetailsValidationError` on facade `python`
- exposed `attachBusinessDetailsValidationError` on facade `ruby`
- exposed `attachBusinessDetailsValidationError` on facade `typescript`
- exposed `attachBusinessDetailsValidationError` on facade `wasm-edge`
- exposed `billingCycle` on facade `go`
- exposed `billingCycle` on facade `node-native`
- exposed `billingCycle` on facade `python`
- exposed `billingCycle` on facade `ruby`
- exposed `billingCycle` on facade `rust`
- exposed `billingCycle` on facade `typescript`
- exposed `billingCycle` on facade `wasm-browser`
- exposed `billingCycle` on facade `wasm-edge`
- exposed `bootstrapMcpProduct` on facade `capi`
- exposed `bootstrapMcpProduct` on facade `go`
- exposed `bootstrapMcpProduct` on facade `node-native`
- exposed `bootstrapMcpProduct` on facade `python`
- exposed `bootstrapMcpProduct` on facade `ruby`
- exposed `bootstrapMcpProduct` on facade `rust`
- exposed `bootstrapMcpProduct` on facade `typescript`
- exposed `bootstrapMcpProduct` on facade `wasm-edge`
- exposed `buildCreateCustomerParams` on facade `go`
- exposed `buildCreateCustomerParams` on facade `node-native`
- exposed `buildCreateCustomerParams` on facade `python`
- exposed `buildCreateCustomerParams` on facade `ruby`
- exposed `buildCreateCustomerParams` on facade `typescript`
- exposed `buildCreateCustomerParams` on facade `wasm-edge`
- exposed `buildCustomerSnapshot` on facade `go`
- exposed `buildCustomerSnapshot` on facade `node-native`
- exposed `buildCustomerSnapshot` on facade `python`
- exposed `buildCustomerSnapshot` on facade `python-mcp`
- exposed `buildCustomerSnapshot` on facade `ruby`
- exposed `buildCustomerSnapshot` on facade `ruby-mcp`
- exposed `buildCustomerSnapshot` on facade `rust`
- exposed `buildCustomerSnapshot` on facade `rust-mcp`
- exposed `buildCustomerSnapshot` on facade `typescript`
- exposed `buildCustomerSnapshot` on facade `wasm-edge`
- exposed `buildGateMessage` on facade `go`
- exposed `buildGateMessage` on facade `node-native`
- exposed `buildGateMessage` on facade `python`
- exposed `buildGateMessage` on facade `ruby`
- exposed `buildGateMessage` on facade `rust`
- exposed `buildGateMessage` on facade `typescript`
- exposed `buildGateMessage` on facade `wasm-edge`
- exposed `buildNudgeMessage` on facade `go`
- exposed `buildNudgeMessage` on facade `node-native`
- exposed `buildNudgeMessage` on facade `python`
- exposed `buildNudgeMessage` on facade `ruby`
- exposed `buildNudgeMessage` on facade `rust`
- exposed `buildNudgeMessage` on facade `typescript`
- exposed `buildNudgeMessage` on facade `wasm-edge`
- exposed `buildPayableToolResult` on facade `go`
- exposed `buildPayableToolResult` on facade `node-native`
- exposed `buildPayableToolResult` on facade `python`
- exposed `buildPayableToolResult` on facade `python-mcp`
- exposed `buildPayableToolResult` on facade `ruby`
- exposed `buildPayableToolResult` on facade `ruby-mcp`
- exposed `buildPayableToolResult` on facade `rust-mcp`
- exposed `buildPayableToolResult` on facade `typescript`
- exposed `buildPayableToolResult` on facade `wasm-edge`
- exposed `buildPaywallGate` on facade `go`
- exposed `buildPaywallGate` on facade `node-native`
- exposed `buildPaywallGate` on facade `python`
- exposed `buildPaywallGate` on facade `ruby`
- exposed `buildPaywallGate` on facade `rust`
- exposed `buildPaywallGate` on facade `typescript`
- exposed `buildPaywallGate` on facade `wasm-edge`
- exposed `buildPromptDescriptorMetadata` on facade `go`
- exposed `buildPromptDescriptorMetadata` on facade `node-native`
- exposed `buildPromptDescriptorMetadata` on facade `python`
- exposed `buildPromptDescriptorMetadata` on facade `python-mcp`
- exposed `buildPromptDescriptorMetadata` on facade `ruby`
- exposed `buildPromptDescriptorMetadata` on facade `ruby-mcp`
- exposed `buildPromptDescriptorMetadata` on facade `rust-mcp`
- exposed `buildPromptDescriptorMetadata` on facade `typescript`
- exposed `buildPromptDescriptorMetadata` on facade `wasm-edge`
- exposed `buildPromptUserMessage` on facade `go`
- exposed `buildPromptUserMessage` on facade `node-native`
- exposed `buildPromptUserMessage` on facade `python`
- exposed `buildPromptUserMessage` on facade `python-mcp`
- exposed `buildPromptUserMessage` on facade `ruby`
- exposed `buildPromptUserMessage` on facade `ruby-mcp`
- exposed `buildPromptUserMessage` on facade `rust-mcp`
- exposed `buildPromptUserMessage` on facade `typescript`
- exposed `buildPromptUserMessage` on facade `wasm-edge`
- exposed `buildToolDescriptorMetadata` on facade `go`
- exposed `buildToolDescriptorMetadata` on facade `node-native`
- exposed `buildToolDescriptorMetadata` on facade `python`
- exposed `buildToolDescriptorMetadata` on facade `python-mcp`
- exposed `buildToolDescriptorMetadata` on facade `ruby`
- exposed `buildToolDescriptorMetadata` on facade `ruby-mcp`
- exposed `buildToolDescriptorMetadata` on facade `rust-mcp`
- exposed `buildToolDescriptorMetadata` on facade `typescript`
- exposed `buildToolDescriptorMetadata` on facade `wasm-edge`
- exposed `cancelPurchase` on facade `capi`
- exposed `cancelPurchase` on facade `go`
- exposed `cancelPurchase` on facade `node-native`
- exposed `cancelPurchase` on facade `python`
- exposed `cancelPurchase` on facade `ruby`
- exposed `cancelPurchase` on facade `rust`
- exposed `cancelPurchase` on facade `typescript`
- exposed `cancelPurchase` on facade `wasm-edge`
- exposed `charges` on facade `go`
- exposed `charges` on facade `node-native`
- exposed `charges` on facade `python`
- exposed `charges` on facade `ruby`
- exposed `charges` on facade `rust`
- exposed `charges` on facade `typescript`
- exposed `charges` on facade `wasm-browser`
- exposed `charges` on facade `wasm-edge`
- exposed `checkLimits` on facade `capi`
- exposed `checkLimits` on facade `go`
- exposed `checkLimits` on facade `node-native`
- exposed `checkLimits` on facade `python`
- exposed `checkLimits` on facade `python-mcp`
- exposed `checkLimits` on facade `ruby`
- exposed `checkLimits` on facade `rust`
- exposed `checkLimits` on facade `typescript`
- exposed `checkLimits` on facade `wasm-edge`
- exposed `classifyCancelError` on facade `go`
- exposed `classifyCancelError` on facade `node-native`
- exposed `classifyCancelError` on facade `python`
- exposed `classifyCancelError` on facade `ruby`
- exposed `classifyCancelError` on facade `typescript`
- exposed `classifyCancelError` on facade `wasm-edge`
- exposed `classifyCreateError` on facade `go`
- exposed `classifyCreateError` on facade `node-native`
- exposed `classifyCreateError` on facade `python`
- exposed `classifyCreateError` on facade `ruby`
- exposed `classifyCreateError` on facade `typescript`
- exposed `classifyCreateError` on facade `wasm-edge`
- exposed `classifyCustomerRef` on facade `go`
- exposed `classifyCustomerRef` on facade `node-native`
- exposed `classifyCustomerRef` on facade `python`
- exposed `classifyCustomerRef` on facade `ruby`
- exposed `classifyCustomerRef` on facade `typescript`
- exposed `classifyCustomerRef` on facade `wasm-edge`
- exposed `classifyLookupError` on facade `go`
- exposed `classifyLookupError` on facade `node-native`
- exposed `classifyLookupError` on facade `python`
- exposed `classifyLookupError` on facade `ruby`
- exposed `classifyLookupError` on facade `typescript`
- exposed `classifyLookupError` on facade `wasm-edge`
- exposed `classifyPaywallState` on facade `go`
- exposed `classifyPaywallState` on facade `node-native`
- exposed `classifyPaywallState` on facade `python`
- exposed `classifyPaywallState` on facade `ruby`
- exposed `classifyPaywallState` on facade `rust`
- exposed `classifyPaywallState` on facade `typescript`
- exposed `classifyPaywallState` on facade `wasm-edge`
- exposed `classifyReactivateError` on facade `go`
- exposed `classifyReactivateError` on facade `node-native`
- exposed `classifyReactivateError` on facade `python`
- exposed `classifyReactivateError` on facade `ruby`
- exposed `classifyReactivateError` on facade `typescript`
- exposed `classifyReactivateError` on facade `wasm-edge`
- exposed `cloneProduct` on facade `capi`
- exposed `cloneProduct` on facade `go`
- exposed `cloneProduct` on facade `node-native`
- exposed `cloneProduct` on facade `python`
- exposed `cloneProduct` on facade `ruby`
- exposed `cloneProduct` on facade `rust`
- exposed `cloneProduct` on facade `typescript`
- exposed `cloneProduct` on facade `wasm-edge`
- exposed `coerceCustomerOptions` on facade `go`
- exposed `coerceCustomerOptions` on facade `node-native`
- exposed `coerceCustomerOptions` on facade `python`
- exposed `coerceCustomerOptions` on facade `ruby`
- exposed `coerceCustomerOptions` on facade `typescript`
- exposed `coerceCustomerOptions` on facade `wasm-edge`
- exposed `compileStringFieldInputSchemaJson` on facade `go`
- exposed `compileStringFieldInputSchemaJson` on facade `node-native`
- exposed `compileStringFieldInputSchemaJson` on facade `python`
- exposed `compileStringFieldInputSchemaJson` on facade `ruby`
- exposed `compileStringFieldInputSchemaJson` on facade `typescript`
- exposed `compileStringFieldInputSchemaJson` on facade `wasm-edge`
- exposed `configureMcpPlans` on facade `capi`
- exposed `configureMcpPlans` on facade `go`
- exposed `configureMcpPlans` on facade `node-native`
- exposed `configureMcpPlans` on facade `python`
- exposed `configureMcpPlans` on facade `ruby`
- exposed `configureMcpPlans` on facade `rust`
- exposed `configureMcpPlans` on facade `typescript`
- exposed `configureMcpPlans` on facade `wasm-edge`
- exposed `countsUsage` on facade `go`
- exposed `countsUsage` on facade `node-native`
- exposed `countsUsage` on facade `python`
- exposed `countsUsage` on facade `ruby`
- exposed `countsUsage` on facade `rust`
- exposed `countsUsage` on facade `typescript`
- exposed `countsUsage` on facade `wasm-browser`
- exposed `countsUsage` on facade `wasm-edge`
- exposed `createCheckoutSession` on facade `capi`
- exposed `createCheckoutSession` on facade `go`
- exposed `createCheckoutSession` on facade `node-native`
- exposed `createCheckoutSession` on facade `python`
- exposed `createCheckoutSession` on facade `ruby`
- exposed `createCheckoutSession` on facade `rust`
- exposed `createCheckoutSession` on facade `typescript`
- exposed `createCheckoutSession` on facade `wasm-edge`
- exposed `createCustomer` on facade `capi`
- exposed `createCustomer` on facade `go`
- exposed `createCustomer` on facade `node-native`
- exposed `createCustomer` on facade `python`
- exposed `createCustomer` on facade `python-mcp`
- exposed `createCustomer` on facade `ruby`
- exposed `createCustomer` on facade `rust`
- exposed `createCustomer` on facade `typescript`
- exposed `createCustomer` on facade `wasm-edge`
- exposed `createCustomerSession` on facade `capi`
- exposed `createCustomerSession` on facade `go`
- exposed `createCustomerSession` on facade `node-native`
- exposed `createCustomerSession` on facade `python`
- exposed `createCustomerSession` on facade `ruby`
- exposed `createCustomerSession` on facade `rust`
- exposed `createCustomerSession` on facade `typescript`
- exposed `createCustomerSession` on facade `wasm-edge`
- exposed `createPaymentIntent` on facade `capi`
- exposed `createPaymentIntent` on facade `go`
- exposed `createPaymentIntent` on facade `node-native`
- exposed `createPaymentIntent` on facade `python`
- exposed `createPaymentIntent` on facade `ruby`
- exposed `createPaymentIntent` on facade `rust`
- exposed `createPaymentIntent` on facade `typescript`
- exposed `createPaymentIntent` on facade `wasm-edge`
- exposed `createPlan` on facade `capi`
- exposed `createPlan` on facade `go`
- exposed `createPlan` on facade `node-native`
- exposed `createPlan` on facade `python`
- exposed `createPlan` on facade `ruby`
- exposed `createPlan` on facade `rust`
- exposed `createPlan` on facade `typescript`
- exposed `createPlan` on facade `wasm-edge`
- exposed `createProduct` on facade `capi`
- exposed `createProduct` on facade `go`
- exposed `createProduct` on facade `node-native`
- exposed `createProduct` on facade `python`
- exposed `createProduct` on facade `ruby`
- exposed `createProduct` on facade `rust`
- exposed `createProduct` on facade `typescript`
- exposed `createProduct` on facade `wasm-edge`
- exposed `createTopupPaymentIntent` on facade `capi`
- exposed `createTopupPaymentIntent` on facade `go`
- exposed `createTopupPaymentIntent` on facade `node-native`
- exposed `createTopupPaymentIntent` on facade `python`
- exposed `createTopupPaymentIntent` on facade `ruby`
- exposed `createTopupPaymentIntent` on facade `rust`
- exposed `createTopupPaymentIntent` on facade `typescript`
- exposed `createTopupPaymentIntent` on facade `wasm-edge`
- exposed `creditSignals` on facade `go`
- exposed `creditSignals` on facade `node-native`
- exposed `creditSignals` on facade `python`
- exposed `creditSignals` on facade `ruby`
- exposed `creditSignals` on facade `rust`
- exposed `creditSignals` on facade `typescript`
- exposed `creditSignals` on facade `wasm-edge`
- exposed `creditsPerUnitFromBalance` on facade `go`
- exposed `creditsPerUnitFromBalance` on facade `node-native`
- exposed `creditsPerUnitFromBalance` on facade `python`
- exposed `creditsPerUnitFromBalance` on facade `ruby`
- exposed `creditsPerUnitFromBalance` on facade `rust`
- exposed `creditsPerUnitFromBalance` on facade `typescript`
- exposed `creditsPerUnitFromBalance` on facade `wasm-browser`
- exposed `creditsPerUnitFromBalance` on facade `wasm-edge`
- exposed `creditsToDisplayMinorUnits` on facade `go`
- exposed `creditsToDisplayMinorUnits` on facade `node-native`
- exposed `creditsToDisplayMinorUnits` on facade `python`
- exposed `creditsToDisplayMinorUnits` on facade `ruby`
- exposed `creditsToDisplayMinorUnits` on facade `rust`
- exposed `creditsToDisplayMinorUnits` on facade `typescript`
- exposed `creditsToDisplayMinorUnits` on facade `wasm-browser`
- exposed `creditsToDisplayMinorUnits` on facade `wasm-edge`
- exposed `customerRefFromClaims` on facade `go`
- exposed `customerRefFromClaims` on facade `node-native`
- exposed `customerRefFromClaims` on facade `python`
- exposed `customerRefFromClaims` on facade `python-mcp`
- exposed `customerRefFromClaims` on facade `ruby`
- exposed `customerRefFromClaims` on facade `typescript`
- exposed `customerRefFromClaims` on facade `wasm-edge`
- exposed `decidePaywallOutcome` on facade `go`
- exposed `decidePaywallOutcome` on facade `node-native`
- exposed `decidePaywallOutcome` on facade `python`
- exposed `decidePaywallOutcome` on facade `ruby`
- exposed `decidePaywallOutcome` on facade `typescript`
- exposed `decidePaywallOutcome` on facade `wasm-edge`
- exposed `decodeJwtPayloadUnverified` on facade `go`
- exposed `decodeJwtPayloadUnverified` on facade `node-native`
- exposed `decodeJwtPayloadUnverified` on facade `python`
- exposed `decodeJwtPayloadUnverified` on facade `python-mcp`
- exposed `decodeJwtPayloadUnverified` on facade `ruby`
- exposed `decodeJwtPayloadUnverified` on facade `typescript`
- exposed `decodeJwtPayloadUnverified` on facade `wasm-edge`
- exposed `defaultMcpBearerExpectations` on facade `go`
- exposed `defaultMcpBearerExpectations` on facade `node-native`
- exposed `defaultMcpBearerExpectations` on facade `python`
- exposed `defaultMcpBearerExpectations` on facade `python-mcp`
- exposed `defaultMcpBearerExpectations` on facade `ruby`
- exposed `defaultMcpBearerExpectations` on facade `typescript`
- exposed `defaultMcpBearerExpectations` on facade `wasm-edge`
- exposed `deletePlan` on facade `capi`
- exposed `deletePlan` on facade `go`
- exposed `deletePlan` on facade `node-native`
- exposed `deletePlan` on facade `python`
- exposed `deletePlan` on facade `ruby`
- exposed `deletePlan` on facade `rust`
- exposed `deletePlan` on facade `typescript`
- exposed `deletePlan` on facade `wasm-edge`
- exposed `deleteProduct` on facade `capi`
- exposed `deleteProduct` on facade `go`
- exposed `deleteProduct` on facade `node-native`
- exposed `deleteProduct` on facade `python`
- exposed `deleteProduct` on facade `ruby`
- exposed `deleteProduct` on facade `rust`
- exposed `deleteProduct` on facade `typescript`
- exposed `deleteProduct` on facade `wasm-edge`
- exposed `deriveActiveProducts` on facade `go`
- exposed `deriveActiveProducts` on facade `node-native`
- exposed `deriveActiveProducts` on facade `python`
- exposed `deriveActiveProducts` on facade `ruby`
- exposed `deriveActiveProducts` on facade `typescript`
- exposed `deriveActiveProducts` on facade `wasm-browser`
- exposed `deriveActiveProducts` on facade `wasm-edge`
- exposed `deriveDefaultView` on facade `go`
- exposed `deriveDefaultView` on facade `node-native`
- exposed `deriveDefaultView` on facade `python`
- exposed `deriveDefaultView` on facade `ruby`
- exposed `deriveDefaultView` on facade `typescript`
- exposed `deriveDefaultView` on facade `wasm-browser`
- exposed `deriveDefaultView` on facade `wasm-edge`
- exposed `deriveIcons` on facade `go`
- exposed `deriveIcons` on facade `node-native`
- exposed `deriveIcons` on facade `python`
- exposed `deriveIcons` on facade `python-mcp`
- exposed `deriveIcons` on facade `ruby`
- exposed `deriveIcons` on facade `ruby-mcp`
- exposed `deriveIcons` on facade `rust-mcp`
- exposed `deriveIcons` on facade `typescript`
- exposed `deriveIcons` on facade `wasm-edge`
- exposed `deriveTaxIdType` on facade `go`
- exposed `deriveTaxIdType` on facade `node-native`
- exposed `deriveTaxIdType` on facade `python`
- exposed `deriveTaxIdType` on facade `ruby`
- exposed `deriveTaxIdType` on facade `rust`
- exposed `deriveTaxIdType` on facade `typescript`
- exposed `deriveTaxIdType` on facade `wasm-browser`
- exposed `deriveTaxIdType` on facade `wasm-edge`
- exposed `disableAutoRecharge` on facade `capi`
- exposed `disableAutoRecharge` on facade `go`
- exposed `disableAutoRecharge` on facade `node-native`
- exposed `disableAutoRecharge` on facade `python`
- exposed `disableAutoRecharge` on facade `ruby`
- exposed `disableAutoRecharge` on facade `rust`
- exposed `disableAutoRecharge` on facade `typescript`
- exposed `disableAutoRecharge` on facade `wasm-edge`
- exposed `ensureCustomerNext` on facade `go`
- exposed `ensureCustomerNext` on facade `node-native`
- exposed `ensureCustomerNext` on facade `python`
- exposed `ensureCustomerNext` on facade `python-mcp`
- exposed `ensureCustomerNext` on facade `ruby`
- exposed `ensureCustomerNext` on facade `rust`
- exposed `ensureCustomerNext` on facade `typescript`
- exposed `ensureCustomerNext` on facade `wasm-edge`
- exposed `ensureOutputSchemaObjectType` on facade `go`
- exposed `ensureOutputSchemaObjectType` on facade `node-native`
- exposed `ensureOutputSchemaObjectType` on facade `python`
- exposed `ensureOutputSchemaObjectType` on facade `python-mcp`
- exposed `ensureOutputSchemaObjectType` on facade `ruby`
- exposed `ensureOutputSchemaObjectType` on facade `typescript`
- exposed `ensureOutputSchemaObjectType` on facade `wasm-edge`
- exposed `evaluateBalanceObservation` on facade `go`
- exposed `evaluateBalanceObservation` on facade `node-native`
- exposed `evaluateBalanceObservation` on facade `python`
- exposed `evaluateBalanceObservation` on facade `ruby`
- exposed `evaluateBalanceObservation` on facade `typescript`
- exposed `evaluateBalanceObservation` on facade `wasm-edge`
- exposed `evaluateCachedLimits` on facade `go`
- exposed `evaluateCachedLimits` on facade `node-native`
- exposed `evaluateCachedLimits` on facade `python`
- exposed `evaluateCachedLimits` on facade `ruby`
- exposed `evaluateCachedLimits` on facade `typescript`
- exposed `evaluateCachedLimits` on facade `wasm-edge`
- exposed `evaluateClaimedLimits` on facade `go`
- exposed `evaluateClaimedLimits` on facade `node-native`
- exposed `evaluateClaimedLimits` on facade `python`
- exposed `evaluateClaimedLimits` on facade `ruby`
- exposed `evaluateClaimedLimits` on facade `typescript`
- exposed `evaluateClaimedLimits` on facade `wasm-edge`
- exposed `evaluateFreshLimits` on facade `go`
- exposed `evaluateFreshLimits` on facade `node-native`
- exposed `evaluateFreshLimits` on facade `python`
- exposed `evaluateFreshLimits` on facade `ruby`
- exposed `evaluateFreshLimits` on facade `typescript`
- exposed `evaluateFreshLimits` on facade `wasm-edge`
- exposed `evaluateProductReadiness` on facade `go`
- exposed `evaluateProductReadiness` on facade `node-native`
- exposed `evaluateProductReadiness` on facade `python`
- exposed `evaluateProductReadiness` on facade `ruby`
- exposed `evaluateProductReadiness` on facade `rust`
- exposed `evaluateProductReadiness` on facade `typescript`
- exposed `evaluateProductReadiness` on facade `wasm-edge`
- exposed `extractBackendCustomerRef` on facade `go`
- exposed `extractBackendCustomerRef` on facade `node-native`
- exposed `extractBackendCustomerRef` on facade `python`
- exposed `extractBackendCustomerRef` on facade `ruby`
- exposed `extractBackendCustomerRef` on facade `typescript`
- exposed `extractBackendCustomerRef` on facade `wasm-edge`
- exposed `extractBearerToken` on facade `go`
- exposed `extractBearerToken` on facade `node-native`
- exposed `extractBearerToken` on facade `python`
- exposed `extractBearerToken` on facade `python-mcp`
- exposed `extractBearerToken` on facade `ruby`
- exposed `extractBearerToken` on facade `typescript`
- exposed `extractBearerToken` on facade `wasm-edge`
- exposed `fetchJwks` on facade `capi`
- exposed `fetchJwks` on facade `go`
- exposed `fetchJwks` on facade `node-native`
- exposed `fetchJwks` on facade `python`
- exposed `fetchJwks` on facade `ruby`
- exposed `fetchJwks` on facade `rust`
- exposed `fetchJwks` on facade `typescript`
- exposed `fetchJwks` on facade `wasm-edge`
- exposed `formatCompactCredits` on facade `go`
- exposed `formatCompactCredits` on facade `node-native`
- exposed `formatCompactCredits` on facade `python`
- exposed `formatCompactCredits` on facade `ruby`
- exposed `formatCompactCredits` on facade `typescript`
- exposed `formatCompactCredits` on facade `wasm-browser`
- exposed `formatCompactCredits` on facade `wasm-edge`
- exposed `formatPrice` on facade `go`
- exposed `formatPrice` on facade `node-native`
- exposed `formatPrice` on facade `python`
- exposed `formatPrice` on facade `ruby`
- exposed `formatPrice` on facade `rust`
- exposed `formatPrice` on facade `typescript`
- exposed `formatPrice` on facade `wasm-browser`
- exposed `formatPrice` on facade `wasm-edge`
- exposed `formatSubtotalLabel` on facade `go`
- exposed `formatSubtotalLabel` on facade `node-native`
- exposed `formatSubtotalLabel` on facade `python`
- exposed `formatSubtotalLabel` on facade `ruby`
- exposed `formatSubtotalLabel` on facade `rust`
- exposed `formatSubtotalLabel` on facade `typescript`
- exposed `formatSubtotalLabel` on facade `wasm-browser`
- exposed `formatSubtotalLabel` on facade `wasm-edge`
- exposed `formatVatSummaryLabel` on facade `go`
- exposed `formatVatSummaryLabel` on facade `node-native`
- exposed `formatVatSummaryLabel` on facade `python`
- exposed `formatVatSummaryLabel` on facade `ruby`
- exposed `formatVatSummaryLabel` on facade `rust`
- exposed `formatVatSummaryLabel` on facade `typescript`
- exposed `formatVatSummaryLabel` on facade `wasm-browser`
- exposed `formatVatSummaryLabel` on facade `wasm-edge`
- exposed `freeLimitsAgree` on facade `go`
- exposed `freeLimitsAgree` on facade `node-native`
- exposed `freeLimitsAgree` on facade `python`
- exposed `freeLimitsAgree` on facade `ruby`
- exposed `freeLimitsAgree` on facade `rust`
- exposed `freeLimitsAgree` on facade `typescript`
- exposed `freeLimitsAgree` on facade `wasm-edge`
- exposed `freeMeterNamePattern` on facade `go`
- exposed `freeMeterNamePattern` on facade `node-native`
- exposed `freeMeterNamePattern` on facade `python`
- exposed `freeMeterNamePattern` on facade `ruby`
- exposed `freeMeterNamePattern` on facade `rust`
- exposed `freeMeterNamePattern` on facade `typescript`
- exposed `freeMeterNamePattern` on facade `wasm-edge`
- exposed `freeToolDescriptionSuffix` on facade `go`
- exposed `freeToolDescriptionSuffix` on facade `node-native`
- exposed `freeToolDescriptionSuffix` on facade `python`
- exposed `freeToolDescriptionSuffix` on facade `ruby`
- exposed `freeToolDescriptionSuffix` on facade `rust`
- exposed `freeToolDescriptionSuffix` on facade `typescript`
- exposed `freeToolDescriptionSuffix` on facade `wasm-edge`
- exposed `gateNext` on facade `go`
- exposed `gateNext` on facade `node-native`
- exposed `gateNext` on facade `python`
- exposed `gateNext` on facade `python-mcp`
- exposed `gateNext` on facade `ruby`
- exposed `gateNext` on facade `rust`
- exposed `gateNext` on facade `typescript`
- exposed `gateNext` on facade `wasm-edge`
- exposed `getAutoRecharge` on facade `capi`
- exposed `getAutoRecharge` on facade `go`
- exposed `getAutoRecharge` on facade `node-native`
- exposed `getAutoRecharge` on facade `python`
- exposed `getAutoRecharge` on facade `ruby`
- exposed `getAutoRecharge` on facade `rust`
- exposed `getAutoRecharge` on facade `typescript`
- exposed `getAutoRecharge` on facade `wasm-edge`
- exposed `getBusinessCountryOptions` on facade `go`
- exposed `getBusinessCountryOptions` on facade `node-native`
- exposed `getBusinessCountryOptions` on facade `python`
- exposed `getBusinessCountryOptions` on facade `ruby`
- exposed `getBusinessCountryOptions` on facade `typescript`
- exposed `getBusinessCountryOptions` on facade `wasm-browser`
- exposed `getBusinessCountryOptions` on facade `wasm-edge`
- exposed `getCreditActivity` on facade `capi`
- exposed `getCreditActivity` on facade `go`
- exposed `getCreditActivity` on facade `node-native`
- exposed `getCreditActivity` on facade `python`
- exposed `getCreditActivity` on facade `ruby`
- exposed `getCreditActivity` on facade `rust`
- exposed `getCreditActivity` on facade `typescript`
- exposed `getCreditActivity` on facade `wasm-edge`
- exposed `getCustomer` on facade `capi`
- exposed `getCustomer` on facade `go`
- exposed `getCustomer` on facade `node-native`
- exposed `getCustomer` on facade `python`
- exposed `getCustomer` on facade `python-mcp`
- exposed `getCustomer` on facade `ruby`
- exposed `getCustomer` on facade `rust`
- exposed `getCustomer` on facade `typescript`
- exposed `getCustomer` on facade `wasm-edge`
- exposed `getCustomerAddressFieldErrors` on facade `go`
- exposed `getCustomerAddressFieldErrors` on facade `node-native`
- exposed `getCustomerAddressFieldErrors` on facade `python`
- exposed `getCustomerAddressFieldErrors` on facade `ruby`
- exposed `getCustomerAddressFieldErrors` on facade `typescript`
- exposed `getCustomerAddressFieldErrors` on facade `wasm-browser`
- exposed `getCustomerAddressFieldErrors` on facade `wasm-edge`
- exposed `getCustomerBalance` on facade `capi`
- exposed `getCustomerBalance` on facade `go`
- exposed `getCustomerBalance` on facade `node-native`
- exposed `getCustomerBalance` on facade `python`
- exposed `getCustomerBalance` on facade `ruby`
- exposed `getCustomerBalance` on facade `rust`
- exposed `getCustomerBalance` on facade `typescript`
- exposed `getCustomerBalance` on facade `wasm-edge`
- exposed `getHistoryNext` on facade `go`
- exposed `getHistoryNext` on facade `node-native`
- exposed `getHistoryNext` on facade `python`
- exposed `getHistoryNext` on facade `ruby`
- exposed `getHistoryNext` on facade `typescript`
- exposed `getHistoryNext` on facade `wasm-edge`
- exposed `getMerchant` on facade `capi`
- exposed `getMerchant` on facade `go`
- exposed `getMerchant` on facade `node-native`
- exposed `getMerchant` on facade `python`
- exposed `getMerchant` on facade `ruby`
- exposed `getMerchant` on facade `rust`
- exposed `getMerchant` on facade `typescript`
- exposed `getMerchant` on facade `wasm-edge`
- exposed `getPaymentMethod` on facade `capi`
- exposed `getPaymentMethod` on facade `go`
- exposed `getPaymentMethod` on facade `node-native`
- exposed `getPaymentMethod` on facade `python`
- exposed `getPaymentMethod` on facade `ruby`
- exposed `getPaymentMethod` on facade `rust`
- exposed `getPaymentMethod` on facade `typescript`
- exposed `getPaymentMethod` on facade `wasm-edge`
- exposed `getPlatformConfig` on facade `capi`
- exposed `getPlatformConfig` on facade `go`
- exposed `getPlatformConfig` on facade `node-native`
- exposed `getPlatformConfig` on facade `python`
- exposed `getPlatformConfig` on facade `ruby`
- exposed `getPlatformConfig` on facade `rust`
- exposed `getPlatformConfig` on facade `typescript`
- exposed `getPlatformConfig` on facade `wasm-edge`
- exposed `getPostalCodeFieldLabel` on facade `go`
- exposed `getPostalCodeFieldLabel` on facade `node-native`
- exposed `getPostalCodeFieldLabel` on facade `python`
- exposed `getPostalCodeFieldLabel` on facade `ruby`
- exposed `getPostalCodeFieldLabel` on facade `rust`
- exposed `getPostalCodeFieldLabel` on facade `typescript`
- exposed `getPostalCodeFieldLabel` on facade `wasm-browser`
- exposed `getPostalCodeFieldLabel` on facade `wasm-edge`
- exposed `getPostalCodePlaceholder` on facade `go`
- exposed `getPostalCodePlaceholder` on facade `node-native`
- exposed `getPostalCodePlaceholder` on facade `python`
- exposed `getPostalCodePlaceholder` on facade `ruby`
- exposed `getPostalCodePlaceholder` on facade `rust`
- exposed `getPostalCodePlaceholder` on facade `typescript`
- exposed `getPostalCodePlaceholder` on facade `wasm-browser`
- exposed `getPostalCodePlaceholder` on facade `wasm-edge`
- exposed `getProduct` on facade `capi`
- exposed `getProduct` on facade `go`
- exposed `getProduct` on facade `node-native`
- exposed `getProduct` on facade `python`
- exposed `getProduct` on facade `ruby`
- exposed `getProduct` on facade `rust`
- exposed `getProduct` on facade `typescript`
- exposed `getProduct` on facade `wasm-edge`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `go`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `node-native`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `python`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `ruby`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `rust`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `typescript`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `wasm-browser`
- exposed `getSellerTaxIdentifierDisplayLabel` on facade `wasm-edge`
- exposed `getStateFieldLabel` on facade `go`
- exposed `getStateFieldLabel` on facade `node-native`
- exposed `getStateFieldLabel` on facade `python`
- exposed `getStateFieldLabel` on facade `ruby`
- exposed `getStateFieldLabel` on facade `rust`
- exposed `getStateFieldLabel` on facade `typescript`
- exposed `getStateFieldLabel` on facade `wasm-browser`
- exposed `getStateFieldLabel` on facade `wasm-edge`
- exposed `getTaxIdExample` on facade `go`
- exposed `getTaxIdExample` on facade `node-native`
- exposed `getTaxIdExample` on facade `python`
- exposed `getTaxIdExample` on facade `ruby`
- exposed `getTaxIdExample` on facade `rust`
- exposed `getTaxIdExample` on facade `typescript`
- exposed `getTaxIdExample` on facade `wasm-browser`
- exposed `getTaxIdExample` on facade `wasm-edge`
- exposed `getTaxIdFieldLabel` on facade `go`
- exposed `getTaxIdFieldLabel` on facade `node-native`
- exposed `getTaxIdFieldLabel` on facade `python`
- exposed `getTaxIdFieldLabel` on facade `ruby`
- exposed `getTaxIdFieldLabel` on facade `rust`
- exposed `getTaxIdFieldLabel` on facade `typescript`
- exposed `getTaxIdFieldLabel` on facade `wasm-browser`
- exposed `getTaxIdFieldLabel` on facade `wasm-edge`
- exposed `getTaxIdHelperText` on facade `go`
- exposed `getTaxIdHelperText` on facade `node-native`
- exposed `getTaxIdHelperText` on facade `python`
- exposed `getTaxIdHelperText` on facade `ruby`
- exposed `getTaxIdHelperText` on facade `rust`
- exposed `getTaxIdHelperText` on facade `typescript`
- exposed `getTaxIdHelperText` on facade `wasm-browser`
- exposed `getTaxIdHelperText` on facade `wasm-edge`
- exposed `getUserInfo` on facade `capi`
- exposed `getUserInfo` on facade `go`
- exposed `getUserInfo` on facade `node-native`
- exposed `getUserInfo` on facade `python`
- exposed `getUserInfo` on facade `ruby`
- exposed `getUserInfo` on facade `rust`
- exposed `getUserInfo` on facade `typescript`
- exposed `getUserInfo` on facade `wasm-edge`
- exposed `headlineCharges` on facade `go`
- exposed `headlineCharges` on facade `node-native`
- exposed `headlineCharges` on facade `python`
- exposed `headlineCharges` on facade `ruby`
- exposed `headlineCharges` on facade `rust`
- exposed `headlineCharges` on facade `typescript`
- exposed `headlineCharges` on facade `wasm-browser`
- exposed `headlineCharges` on facade `wasm-edge`
- exposed `historyRows` on facade `go`
- exposed `historyRows` on facade `node-native`
- exposed `historyRows` on facade `python`
- exposed `historyRows` on facade `ruby`
- exposed `historyRows` on facade `typescript`
- exposed `historyRows` on facade `wasm-browser`
- exposed `historyRows` on facade `wasm-edge`
- exposed `includedUnits` on facade `go`
- exposed `includedUnits` on facade `node-native`
- exposed `includedUnits` on facade `python`
- exposed `includedUnits` on facade `ruby`
- exposed `includedUnits` on facade `rust`
- exposed `includedUnits` on facade `typescript`
- exposed `includedUnits` on facade `wasm-browser`
- exposed `includedUnits` on facade `wasm-edge`
- exposed `invokePayableNext` on facade `go`
- exposed `invokePayableNext` on facade `node-native`
- exposed `invokePayableNext` on facade `python`
- exposed `invokePayableNext` on facade `python-mcp`
- exposed `invokePayableNext` on facade `ruby`
- exposed `invokePayableNext` on facade `ruby-mcp`
- exposed `invokePayableNext` on facade `rust`
- exposed `invokePayableNext` on facade `rust-mcp`
- exposed `invokePayableNext` on facade `typescript`
- exposed `invokePayableNext` on facade `wasm-edge`
- exposed `isCachedCustomerRefValid` on facade `go`
- exposed `isCachedCustomerRefValid` on facade `node-native`
- exposed `isCachedCustomerRefValid` on facade `python`
- exposed `isCachedCustomerRefValid` on facade `ruby`
- exposed `isCachedCustomerRefValid` on facade `typescript`
- exposed `isCachedCustomerRefValid` on facade `wasm-edge`
- exposed `isCustomerAddressComplete` on facade `go`
- exposed `isCustomerAddressComplete` on facade `node-native`
- exposed `isCustomerAddressComplete` on facade `python`
- exposed `isCustomerAddressComplete` on facade `ruby`
- exposed `isCustomerAddressComplete` on facade `typescript`
- exposed `isCustomerAddressComplete` on facade `wasm-browser`
- exposed `isCustomerAddressComplete` on facade `wasm-edge`
- exposed `isEmailConflict` on facade `go`
- exposed `isEmailConflict` on facade `node-native`
- exposed `isEmailConflict` on facade `python`
- exposed `isEmailConflict` on facade `ruby`
- exposed `isEmailConflict` on facade `typescript`
- exposed `isEmailConflict` on facade `wasm-edge`
- exposed `isErrorResult` on facade `go`
- exposed `isErrorResult` on facade `node-native`
- exposed `isErrorResult` on facade `python`
- exposed `isErrorResult` on facade `python-mcp`
- exposed `isErrorResult` on facade `ruby`
- exposed `isErrorResult` on facade `typescript`
- exposed `isErrorResult` on facade `wasm-edge`
- exposed `isPostalCodeRequired` on facade `go`
- exposed `isPostalCodeRequired` on facade `node-native`
- exposed `isPostalCodeRequired` on facade `python`
- exposed `isPostalCodeRequired` on facade `ruby`
- exposed `isPostalCodeRequired` on facade `rust`
- exposed `isPostalCodeRequired` on facade `typescript`
- exposed `isPostalCodeRequired` on facade `wasm-browser`
- exposed `isPostalCodeRequired` on facade `wasm-edge`
- exposed `isStateRequired` on facade `go`
- exposed `isStateRequired` on facade `node-native`
- exposed `isStateRequired` on facade `python`
- exposed `isStateRequired` on facade `ruby`
- exposed `isStateRequired` on facade `rust`
- exposed `isStateRequired` on facade `typescript`
- exposed `isStateRequired` on facade `wasm-browser`
- exposed `isStateRequired` on facade `wasm-edge`
- exposed `isTaxIdType` on facade `go`
- exposed `isTaxIdType` on facade `node-native`
- exposed `isTaxIdType` on facade `python`
- exposed `isTaxIdType` on facade `ruby`
- exposed `isTaxIdType` on facade `rust`
- exposed `isTaxIdType` on facade `typescript`
- exposed `isTaxIdType` on facade `wasm-browser`
- exposed `isTaxIdType` on facade `wasm-edge`
- exposed `isUnlimitedRemaining` on facade `go`
- exposed `isUnlimitedRemaining` on facade `node-native`
- exposed `isUnlimitedRemaining` on facade `python`
- exposed `isUnlimitedRemaining` on facade `ruby`
- exposed `isUnlimitedRemaining` on facade `rust`
- exposed `isUnlimitedRemaining` on facade `typescript`
- exposed `isUnlimitedRemaining` on facade `wasm-browser`
- exposed `isUnlimitedRemaining` on facade `wasm-edge`
- exposed `isZeroDecimalCurrency` on facade `go`
- exposed `isZeroDecimalCurrency` on facade `node-native`
- exposed `isZeroDecimalCurrency` on facade `python`
- exposed `isZeroDecimalCurrency` on facade `ruby`
- exposed `isZeroDecimalCurrency` on facade `rust`
- exposed `isZeroDecimalCurrency` on facade `typescript`
- exposed `isZeroDecimalCurrency` on facade `wasm-browser`
- exposed `isZeroDecimalCurrency` on facade `wasm-edge`
- exposed `linkLabel` on facade `go`
- exposed `linkLabel` on facade `node-native`
- exposed `linkLabel` on facade `python`
- exposed `linkLabel` on facade `ruby`
- exposed `linkLabel` on facade `rust`
- exposed `linkLabel` on facade `typescript`
- exposed `linkLabel` on facade `wasm-edge`
- exposed `listPlans` on facade `capi`
- exposed `listPlans` on facade `go`
- exposed `listPlans` on facade `node-native`
- exposed `listPlans` on facade `python`
- exposed `listPlans` on facade `ruby`
- exposed `listPlans` on facade `rust`
- exposed `listPlans` on facade `typescript`
- exposed `listPlans` on facade `wasm-edge`
- exposed `listProducts` on facade `capi`
- exposed `listProducts` on facade `go`
- exposed `listProducts` on facade `node-native`
- exposed `listProducts` on facade `python`
- exposed `listProducts` on facade `ruby`
- exposed `listProducts` on facade `rust`
- exposed `listProducts` on facade `typescript`
- exposed `listProducts` on facade `wasm-edge`
- exposed `listPurchases` on facade `capi`
- exposed `listPurchases` on facade `go`
- exposed `listPurchases` on facade `node-native`
- exposed `listPurchases` on facade `python`
- exposed `listPurchases` on facade `ruby`
- exposed `listPurchases` on facade `rust`
- exposed `listPurchases` on facade `typescript`
- exposed `listPurchases` on facade `wasm-edge`
- exposed `makeResponseResult` on facade `go`
- exposed `makeResponseResult` on facade `node-native`
- exposed `makeResponseResult` on facade `python`
- exposed `makeResponseResult` on facade `python-mcp`
- exposed `makeResponseResult` on facade `ruby`
- exposed `makeResponseResult` on facade `ruby-mcp`
- exposed `makeResponseResult` on facade `rust-mcp`
- exposed `makeResponseResult` on facade `typescript`
- exposed `makeResponseResult` on facade `wasm-edge`
- exposed `mapRouteError` on facade `go`
- exposed `mapRouteError` on facade `node-native`
- exposed `mapRouteError` on facade `python`
- exposed `mapRouteError` on facade `python-mcp`
- exposed `mapRouteError` on facade `ruby`
- exposed `mapRouteError` on facade `typescript`
- exposed `mapRouteError` on facade `wasm-edge`
- exposed `mcpBootstrap` on facade `capi`
- exposed `mcpBootstrap` on facade `go`
- exposed `mcpBootstrap` on facade `node-native`
- exposed `mcpBootstrap` on facade `python`
- exposed `mcpBootstrap` on facade `ruby`
- exposed `mcpBootstrap` on facade `rust`
- exposed `mcpBootstrap` on facade `typescript`
- exposed `mcpBootstrap` on facade `wasm-edge`
- exposed `mcpCallBuiltinTool` on facade `capi`
- exposed `mcpCallBuiltinTool` on facade `go`
- exposed `mcpCallBuiltinTool` on facade `node-native`
- exposed `mcpCallBuiltinTool` on facade `python`
- exposed `mcpCallBuiltinTool` on facade `ruby`
- exposed `mcpCallBuiltinTool` on facade `rust`
- exposed `mcpCallBuiltinTool` on facade `typescript`
- exposed `mcpCallBuiltinTool` on facade `wasm-edge`
- exposed `mcpDispatch` on facade `capi`
- exposed `mcpDispatch` on facade `go`
- exposed `mcpDispatch` on facade `node-native`
- exposed `mcpDispatch` on facade `python`
- exposed `mcpDispatch` on facade `python-mcp`
- exposed `mcpDispatch` on facade `ruby`
- exposed `mcpDispatch` on facade `ruby-mcp`
- exposed `mcpDispatch` on facade `rust`
- exposed `mcpDispatch` on facade `rust-mcp`
- exposed `mcpDispatch` on facade `typescript`
- exposed `mcpDispatch` on facade `wasm-edge`
- exposed `mcpOauthRequest` on facade `capi`
- exposed `mcpOauthRequest` on facade `go`
- exposed `mcpOauthRequest` on facade `node-native`
- exposed `mcpOauthRequest` on facade `python`
- exposed `mcpOauthRequest` on facade `python-mcp`
- exposed `mcpOauthRequest` on facade `ruby`
- exposed `mcpOauthRequest` on facade `ruby-mcp`
- exposed `mcpOauthRequest` on facade `rust`
- exposed `mcpOauthRequest` on facade `rust-mcp`
- exposed `mcpOauthRequest` on facade `typescript`
- exposed `mcpOauthRequest` on facade `wasm-edge`
- exposed `mcpReadResource` on facade `capi`
- exposed `mcpReadResource` on facade `go`
- exposed `mcpReadResource` on facade `node-native`
- exposed `mcpReadResource` on facade `python`
- exposed `mcpReadResource` on facade `ruby`
- exposed `mcpReadResource` on facade `rust`
- exposed `mcpReadResource` on facade `typescript`
- exposed `mcpReadResource` on facade `wasm-edge`
- exposed `mcpResolveAuth` on facade `capi`
- exposed `mcpResolveAuth` on facade `go`
- exposed `mcpResolveAuth` on facade `node-native`
- exposed `mcpResolveAuth` on facade `python`
- exposed `mcpResolveAuth` on facade `python-mcp`
- exposed `mcpResolveAuth` on facade `ruby`
- exposed `mcpResolveAuth` on facade `rust`
- exposed `mcpResolveAuth` on facade `typescript`
- exposed `mcpResolveAuth` on facade `wasm-edge`
- exposed `mcpViewMaps` on facade `go`
- exposed `mcpViewMaps` on facade `node-native`
- exposed `mcpViewMaps` on facade `python`
- exposed `mcpViewMaps` on facade `python-mcp`
- exposed `mcpViewMaps` on facade `ruby`
- exposed `mcpViewMaps` on facade `ruby-mcp`
- exposed `mcpViewMaps` on facade `rust-mcp`
- exposed `mcpViewMaps` on facade `typescript`
- exposed `mcpViewMaps` on facade `wasm-edge`
- exposed `meterName` on facade `go`
- exposed `meterName` on facade `node-native`
- exposed `meterName` on facade `python`
- exposed `meterName` on facade `python-mcp`
- exposed `meterName` on facade `ruby`
- exposed `meterName` on facade `rust`
- exposed `meterName` on facade `typescript`
- exposed `meterName` on facade `wasm-browser`
- exposed `meterName` on facade `wasm-edge`
- exposed `minorUnitsPerMajor` on facade `go`
- exposed `minorUnitsPerMajor` on facade `node-native`
- exposed `minorUnitsPerMajor` on facade `python`
- exposed `minorUnitsPerMajor` on facade `ruby`
- exposed `minorUnitsPerMajor` on facade `rust`
- exposed `minorUnitsPerMajor` on facade `typescript`
- exposed `minorUnitsPerMajor` on facade `wasm-browser`
- exposed `minorUnitsPerMajor` on facade `wasm-edge`
- exposed `nextActionFor` on facade `go`
- exposed `nextActionFor` on facade `node-native`
- exposed `nextActionFor` on facade `python`
- exposed `nextActionFor` on facade `ruby`
- exposed `nextActionFor` on facade `rust`
- exposed `nextActionFor` on facade `typescript`
- exposed `nextActionFor` on facade `wasm-edge`
- exposed `normalizeCancelResponse` on facade `go`
- exposed `normalizeCancelResponse` on facade `node-native`
- exposed `normalizeCancelResponse` on facade `python`
- exposed `normalizeCancelResponse` on facade `ruby`
- exposed `normalizeCancelResponse` on facade `typescript`
- exposed `normalizeCancelResponse` on facade `wasm-edge`
- exposed `normalizeFreeLimit` on facade `go`
- exposed `normalizeFreeLimit` on facade `node-native`
- exposed `normalizeFreeLimit` on facade `python`
- exposed `normalizeFreeLimit` on facade `ruby`
- exposed `normalizeFreeLimit` on facade `rust`
- exposed `normalizeFreeLimit` on facade `typescript`
- exposed `normalizeFreeLimit` on facade `wasm-edge`
- exposed `normalizeReactivateResponse` on facade `go`
- exposed `normalizeReactivateResponse` on facade `node-native`
- exposed `normalizeReactivateResponse` on facade `python`
- exposed `normalizeReactivateResponse` on facade `ruby`
- exposed `normalizeReactivateResponse` on facade `typescript`
- exposed `normalizeReactivateResponse` on facade `wasm-edge`
- exposed `overlayClaimedLimits` on facade `go`
- exposed `overlayClaimedLimits` on facade `node-native`
- exposed `overlayClaimedLimits` on facade `python`
- exposed `overlayClaimedLimits` on facade `python-mcp`
- exposed `overlayClaimedLimits` on facade `ruby`
- exposed `overlayClaimedLimits` on facade `typescript`
- exposed `overlayClaimedLimits` on facade `wasm-edge`
- exposed `paywallErrorToClientPayload` on facade `go`
- exposed `paywallErrorToClientPayload` on facade `node-native`
- exposed `paywallErrorToClientPayload` on facade `python`
- exposed `paywallErrorToClientPayload` on facade `ruby`
- exposed `paywallErrorToClientPayload` on facade `rust`
- exposed `paywallErrorToClientPayload` on facade `typescript`
- exposed `paywallErrorToClientPayload` on facade `wasm-edge`
- exposed `paywallStructuredContentSchema` on facade `go`
- exposed `paywallStructuredContentSchema` on facade `node-native`
- exposed `paywallStructuredContentSchema` on facade `python`
- exposed `paywallStructuredContentSchema` on facade `ruby`
- exposed `paywallStructuredContentSchema` on facade `rust`
- exposed `paywallStructuredContentSchema` on facade `typescript`
- exposed `paywallStructuredContentSchema` on facade `wasm-edge`
- exposed `paywallToolResult` on facade `go`
- exposed `paywallToolResult` on facade `node-native`
- exposed `paywallToolResult` on facade `python`
- exposed `paywallToolResult` on facade `python-mcp`
- exposed `paywallToolResult` on facade `ruby`
- exposed `paywallToolResult` on facade `ruby-mcp`
- exposed `paywallToolResult` on facade `rust-mcp`
- exposed `paywallToolResult` on facade `typescript`
- exposed `paywallToolResult` on facade `wasm-edge`
- exposed `peggedCreditsPerUnit` on facade `go`
- exposed `peggedCreditsPerUnit` on facade `node-native`
- exposed `peggedCreditsPerUnit` on facade `python`
- exposed `peggedCreditsPerUnit` on facade `ruby`
- exposed `peggedCreditsPerUnit` on facade `rust`
- exposed `peggedCreditsPerUnit` on facade `typescript`
- exposed `peggedCreditsPerUnit` on facade `wasm-browser`
- exposed `peggedCreditsPerUnit` on facade `wasm-edge`
- exposed `perUnitCharge` on facade `go`
- exposed `perUnitCharge` on facade `node-native`
- exposed `perUnitCharge` on facade `python`
- exposed `perUnitCharge` on facade `ruby`
- exposed `perUnitCharge` on facade `rust`
- exposed `perUnitCharge` on facade `typescript`
- exposed `perUnitCharge` on facade `wasm-browser`
- exposed `perUnitCharge` on facade `wasm-edge`
- exposed `planConsequence` on facade `go`
- exposed `planConsequence` on facade `node-native`
- exposed `planConsequence` on facade `python`
- exposed `planConsequence` on facade `ruby`
- exposed `planConsequence` on facade `typescript`
- exposed `planConsequence` on facade `wasm-browser`
- exposed `planConsequence` on facade `wasm-edge`
- exposed `planLadder` on facade `go`
- exposed `planLadder` on facade `node-native`
- exposed `planLadder` on facade `python`
- exposed `planLadder` on facade `ruby`
- exposed `planLadder` on facade `rust`
- exposed `planLadder` on facade `typescript`
- exposed `planLadder` on facade `wasm-edge`
- exposed `planPricingShape` on facade `go`
- exposed `planPricingShape` on facade `node-native`
- exposed `planPricingShape` on facade `python`
- exposed `planPricingShape` on facade `ruby`
- exposed `planPricingShape` on facade `rust`
- exposed `planPricingShape` on facade `typescript`
- exposed `planPricingShape` on facade `wasm-browser`
- exposed `planPricingShape` on facade `wasm-edge`
- exposed `processPaymentIntent` on facade `capi`
- exposed `processPaymentIntent` on facade `go`
- exposed `processPaymentIntent` on facade `node-native`
- exposed `processPaymentIntent` on facade `python`
- exposed `processPaymentIntent` on facade `ruby`
- exposed `processPaymentIntent` on facade `rust`
- exposed `processPaymentIntent` on facade `typescript`
- exposed `processPaymentIntent` on facade `wasm-edge`
- exposed `projectPaymentIntentResult` on facade `go`
- exposed `projectPaymentIntentResult` on facade `node-native`
- exposed `projectPaymentIntentResult` on facade `python`
- exposed `projectPaymentIntentResult` on facade `ruby`
- exposed `projectPaymentIntentResult` on facade `typescript`
- exposed `projectPaymentIntentResult` on facade `wasm-edge`
- exposed `projectTopupProcessOutcome` on facade `go`
- exposed `projectTopupProcessOutcome` on facade `node-native`
- exposed `projectTopupProcessOutcome` on facade `python`
- exposed `projectTopupProcessOutcome` on facade `ruby`
- exposed `projectTopupProcessOutcome` on facade `typescript`
- exposed `projectTopupProcessOutcome` on facade `wasm-edge`
- exposed `projectUsageSnapshot` on facade `go`
- exposed `projectUsageSnapshot` on facade `node-native`
- exposed `projectUsageSnapshot` on facade `python`
- exposed `projectUsageSnapshot` on facade `ruby`
- exposed `projectUsageSnapshot` on facade `typescript`
- exposed `projectUsageSnapshot` on facade `wasm-edge`
- exposed `reactivatePurchase` on facade `capi`
- exposed `reactivatePurchase` on facade `go`
- exposed `reactivatePurchase` on facade `node-native`
- exposed `reactivatePurchase` on facade `python`
- exposed `reactivatePurchase` on facade `ruby`
- exposed `reactivatePurchase` on facade `rust`
- exposed `reactivatePurchase` on facade `typescript`
- exposed `reactivatePurchase` on facade `wasm-edge`
- exposed `requireProductRef` on facade `go`
- exposed `requireProductRef` on facade `node-native`
- exposed `requireProductRef` on facade `python`
- exposed `requireProductRef` on facade `ruby`
- exposed `requireProductRef` on facade `rust`
- exposed `requireProductRef` on facade `typescript`
- exposed `requireProductRef` on facade `wasm-edge`
- exposed `resolveAccountState` on facade `go`
- exposed `resolveAccountState` on facade `node-native`
- exposed `resolveAccountState` on facade `python`
- exposed `resolveAccountState` on facade `ruby`
- exposed `resolveAccountState` on facade `typescript`
- exposed `resolveAccountState` on facade `wasm-browser`
- exposed `resolveAccountState` on facade `wasm-edge`
- exposed `resolveAuthenticatedUser` on facade `go`
- exposed `resolveAuthenticatedUser` on facade `node-native`
- exposed `resolveAuthenticatedUser` on facade `python`
- exposed `resolveAuthenticatedUser` on facade `ruby`
- exposed `resolveAuthenticatedUser` on facade `typescript`
- exposed `resolveAuthenticatedUser` on facade `wasm-edge`
- exposed `resolveBuyerCountry` on facade `go`
- exposed `resolveBuyerCountry` on facade `node-native`
- exposed `resolveBuyerCountry` on facade `python`
- exposed `resolveBuyerCountry` on facade `ruby`
- exposed `resolveBuyerCountry` on facade `typescript`
- exposed `resolveBuyerCountry` on facade `wasm-browser`
- exposed `resolveBuyerCountry` on facade `wasm-edge`
- exposed `resolveCheckLimitsParams` on facade `go`
- exposed `resolveCheckLimitsParams` on facade `node-native`
- exposed `resolveCheckLimitsParams` on facade `python`
- exposed `resolveCheckLimitsParams` on facade `ruby`
- exposed `resolveCheckLimitsParams` on facade `typescript`
- exposed `resolveCheckLimitsParams` on facade `wasm-edge`
- exposed `resolveCustomerRef` on facade `go`
- exposed `resolveCustomerRef` on facade `node-native`
- exposed `resolveCustomerRef` on facade `python`
- exposed `resolveCustomerRef` on facade `python-mcp`
- exposed `resolveCustomerRef` on facade `ruby`
- exposed `resolveCustomerRef` on facade `ruby-mcp`
- exposed `resolveCustomerRef` on facade `rust-mcp`
- exposed `resolveCustomerRef` on facade `typescript`
- exposed `resolveCustomerRef` on facade `wasm-edge`
- exposed `resolveDisplayMode` on facade `go`
- exposed `resolveDisplayMode` on facade `node-native`
- exposed `resolveDisplayMode` on facade `python`
- exposed `resolveDisplayMode` on facade `ruby`
- exposed `resolveDisplayMode` on facade `typescript`
- exposed `resolveDisplayMode` on facade `wasm-browser`
- exposed `resolveDisplayMode` on facade `wasm-edge`
- exposed `resolveFallbackGateLimits` on facade `go`
- exposed `resolveFallbackGateLimits` on facade `node-native`
- exposed `resolveFallbackGateLimits` on facade `python`
- exposed `resolveFallbackGateLimits` on facade `ruby`
- exposed `resolveFallbackGateLimits` on facade `typescript`
- exposed `resolveFallbackGateLimits` on facade `wasm-edge`
- exposed `resolvePlanShape` on facade `go`
- exposed `resolvePlanShape` on facade `node-native`
- exposed `resolvePlanShape` on facade `python`
- exposed `resolvePlanShape` on facade `ruby`
- exposed `resolvePlanShape` on facade `typescript`
- exposed `resolvePlanShape` on facade `wasm-browser`
- exposed `resolvePlanShape` on facade `wasm-edge`
- exposed `resolveProductRef` on facade `go`
- exposed `resolveProductRef` on facade `node-native`
- exposed `resolveProductRef` on facade `python`
- exposed `resolveProductRef` on facade `ruby`
- exposed `resolveProductRef` on facade `typescript`
- exposed `resolveProductRef` on facade `wasm-edge`
- exposed `resolvePurchaseCustomerRef` on facade `go`
- exposed `resolvePurchaseCustomerRef` on facade `node-native`
- exposed `resolvePurchaseCustomerRef` on facade `python`
- exposed `resolvePurchaseCustomerRef` on facade `ruby`
- exposed `resolvePurchaseCustomerRef` on facade `typescript`
- exposed `resolvePurchaseCustomerRef` on facade `wasm-edge`
- exposed `resolveReturnUrl` on facade `go`
- exposed `resolveReturnUrl` on facade `node-native`
- exposed `resolveReturnUrl` on facade `python`
- exposed `resolveReturnUrl` on facade `ruby`
- exposed `resolveReturnUrl` on facade `typescript`
- exposed `resolveReturnUrl` on facade `wasm-edge`
- exposed `resolveSellerIdentityDisplay` on facade `go`
- exposed `resolveSellerIdentityDisplay` on facade `node-native`
- exposed `resolveSellerIdentityDisplay` on facade `python`
- exposed `resolveSellerIdentityDisplay` on facade `ruby`
- exposed `resolveSellerIdentityDisplay` on facade `rust`
- exposed `resolveSellerIdentityDisplay` on facade `typescript`
- exposed `resolveSellerIdentityDisplay` on facade `wasm-browser`
- exposed `resolveSellerIdentityDisplay` on facade `wasm-edge`
- exposed `resolveTaxBehavior` on facade `go`
- exposed `resolveTaxBehavior` on facade `node-native`
- exposed `resolveTaxBehavior` on facade `python`
- exposed `resolveTaxBehavior` on facade `ruby`
- exposed `resolveTaxBehavior` on facade `rust`
- exposed `resolveTaxBehavior` on facade `typescript`
- exposed `resolveTaxBehavior` on facade `wasm-browser`
- exposed `resolveTaxBehavior` on facade `wasm-edge`
- exposed `resolveTaxTreatmentNote` on facade `go`
- exposed `resolveTaxTreatmentNote` on facade `node-native`
- exposed `resolveTaxTreatmentNote` on facade `python`
- exposed `resolveTaxTreatmentNote` on facade `ruby`
- exposed `resolveTaxTreatmentNote` on facade `rust`
- exposed `resolveTaxTreatmentNote` on facade `typescript`
- exposed `resolveTaxTreatmentNote` on facade `wasm-browser`
- exposed `resolveTaxTreatmentNote` on facade `wasm-edge`
- exposed `resolveUsageExtra` on facade `go`
- exposed `resolveUsageExtra` on facade `node-native`
- exposed `resolveUsageExtra` on facade `python`
- exposed `resolveUsageExtra` on facade `ruby`
- exposed `resolveUsageExtra` on facade `typescript`
- exposed `resolveUsageExtra` on facade `wasm-edge`
- exposed `retryNextDelayMs` on facade `go`
- exposed `retryNextDelayMs` on facade `node-native`
- exposed `retryNextDelayMs` on facade `python`
- exposed `retryNextDelayMs` on facade `ruby`
- exposed `retryNextDelayMs` on facade `typescript`
- exposed `retryNextDelayMs` on facade `wasm-edge`
- exposed `saveAutoRecharge` on facade `capi`
- exposed `saveAutoRecharge` on facade `go`
- exposed `saveAutoRecharge` on facade `node-native`
- exposed `saveAutoRecharge` on facade `python`
- exposed `saveAutoRecharge` on facade `ruby`
- exposed `saveAutoRecharge` on facade `rust`
- exposed `saveAutoRecharge` on facade `typescript`
- exposed `saveAutoRecharge` on facade `wasm-edge`
- exposed `selectActivePlanPurchase` on facade `go`
- exposed `selectActivePlanPurchase` on facade `node-native`
- exposed `selectActivePlanPurchase` on facade `python`
- exposed `selectActivePlanPurchase` on facade `ruby`
- exposed `selectActivePlanPurchase` on facade `typescript`
- exposed `selectActivePlanPurchase` on facade `wasm-edge`
- exposed `selectActivePurchases` on facade `go`
- exposed `selectActivePurchases` on facade `node-native`
- exposed `selectActivePurchases` on facade `python`
- exposed `selectActivePurchases` on facade `ruby`
- exposed `selectActivePurchases` on facade `typescript`
- exposed `selectActivePurchases` on facade `wasm-edge`
- exposed `shouldRetryUsageError` on facade `go`
- exposed `shouldRetryUsageError` on facade `node-native`
- exposed `shouldRetryUsageError` on facade `python`
- exposed `shouldRetryUsageError` on facade `ruby`
- exposed `shouldRetryUsageError` on facade `rust`
- exposed `shouldRetryUsageError` on facade `typescript`
- exposed `shouldRetryUsageError` on facade `wasm-edge`
- exposed `shouldShowTaxRow` on facade `go`
- exposed `shouldShowTaxRow` on facade `node-native`
- exposed `shouldShowTaxRow` on facade `python`
- exposed `shouldShowTaxRow` on facade `ruby`
- exposed `shouldShowTaxRow` on facade `rust`
- exposed `shouldShowTaxRow` on facade `typescript`
- exposed `shouldShowTaxRow` on facade `wasm-browser`
- exposed `shouldShowTaxRow` on facade `wasm-edge`
- exposed `tierBands` on facade `go`
- exposed `tierBands` on facade `node-native`
- exposed `tierBands` on facade `python`
- exposed `tierBands` on facade `ruby`
- exposed `tierBands` on facade `rust`
- exposed `tierBands` on facade `typescript`
- exposed `tierBands` on facade `wasm-edge`
- exposed `tierMeters` on facade `go`
- exposed `tierMeters` on facade `node-native`
- exposed `tierMeters` on facade `python`
- exposed `tierMeters` on facade `ruby`
- exposed `tierMeters` on facade `rust`
- exposed `tierMeters` on facade `typescript`
- exposed `tierMeters` on facade `wasm-edge`
- exposed `toMajorUnits` on facade `go`
- exposed `toMajorUnits` on facade `node-native`
- exposed `toMajorUnits` on facade `python`
- exposed `toMajorUnits` on facade `ruby`
- exposed `toMajorUnits` on facade `rust`
- exposed `toMajorUnits` on facade `typescript`
- exposed `toMajorUnits` on facade `wasm-browser`
- exposed `toMajorUnits` on facade `wasm-edge`
- exposed `topupProcessNext` on facade `go`
- exposed `topupProcessNext` on facade `node-native`
- exposed `topupProcessNext` on facade `python`
- exposed `topupProcessNext` on facade `ruby`
- exposed `topupProcessNext` on facade `typescript`
- exposed `topupProcessNext` on facade `wasm-edge`
- exposed `trackUsage` on facade `capi`
- exposed `trackUsage` on facade `go`
- exposed `trackUsage` on facade `node-native`
- exposed `trackUsage` on facade `python`
- exposed `trackUsage` on facade `python-mcp`
- exposed `trackUsage` on facade `ruby`
- exposed `trackUsage` on facade `ruby-mcp`
- exposed `trackUsage` on facade `rust`
- exposed `trackUsage` on facade `rust-mcp`
- exposed `trackUsage` on facade `typescript`
- exposed `trackUsage` on facade `wasm-edge`
- exposed `trackUsageBulk` on facade `capi`
- exposed `trackUsageBulk` on facade `go`
- exposed `trackUsageBulk` on facade `node-native`
- exposed `trackUsageBulk` on facade `python`
- exposed `trackUsageBulk` on facade `ruby`
- exposed `trackUsageBulk` on facade `rust`
- exposed `trackUsageBulk` on facade `typescript`
- exposed `trackUsageBulk` on facade `wasm-edge`
- exposed `trialDays` on facade `go`
- exposed `trialDays` on facade `node-native`
- exposed `trialDays` on facade `python`
- exposed `trialDays` on facade `ruby`
- exposed `trialDays` on facade `rust`
- exposed `trialDays` on facade `typescript`
- exposed `trialDays` on facade `wasm-browser`
- exposed `trialDays` on facade `wasm-edge`
- exposed `updateCustomer` on facade `capi`
- exposed `updateCustomer` on facade `go`
- exposed `updateCustomer` on facade `node-native`
- exposed `updateCustomer` on facade `python`
- exposed `updateCustomer` on facade `python-mcp`
- exposed `updateCustomer` on facade `ruby`
- exposed `updateCustomer` on facade `rust`
- exposed `updateCustomer` on facade `typescript`
- exposed `updateCustomer` on facade `wasm-edge`
- exposed `updatePlan` on facade `capi`
- exposed `updatePlan` on facade `go`
- exposed `updatePlan` on facade `node-native`
- exposed `updatePlan` on facade `python`
- exposed `updatePlan` on facade `ruby`
- exposed `updatePlan` on facade `rust`
- exposed `updatePlan` on facade `typescript`
- exposed `updatePlan` on facade `wasm-edge`
- exposed `updateProduct` on facade `capi`
- exposed `updateProduct` on facade `go`
- exposed `updateProduct` on facade `node-native`
- exposed `updateProduct` on facade `python`
- exposed `updateProduct` on facade `ruby`
- exposed `updateProduct` on facade `rust`
- exposed `updateProduct` on facade `typescript`
- exposed `updateProduct` on facade `wasm-edge`
- exposed `usageRate` on facade `go`
- exposed `usageRate` on facade `node-native`
- exposed `usageRate` on facade `python`
- exposed `usageRate` on facade `ruby`
- exposed `usageRate` on facade `rust`
- exposed `usageRate` on facade `typescript`
- exposed `usageRate` on facade `wasm-browser`
- exposed `usageRate` on facade `wasm-edge`
- exposed `validateActivatePlanParams` on facade `go`
- exposed `validateActivatePlanParams` on facade `node-native`
- exposed `validateActivatePlanParams` on facade `python`
- exposed `validateActivatePlanParams` on facade `ruby`
- exposed `validateActivatePlanParams` on facade `typescript`
- exposed `validateActivatePlanParams` on facade `wasm-edge`
- exposed `validateAttachBusinessDetailsParams` on facade `go`
- exposed `validateAttachBusinessDetailsParams` on facade `node-native`
- exposed `validateAttachBusinessDetailsParams` on facade `python`
- exposed `validateAttachBusinessDetailsParams` on facade `ruby`
- exposed `validateAttachBusinessDetailsParams` on facade `typescript`
- exposed `validateAttachBusinessDetailsParams` on facade `wasm-edge`
- exposed `validateBusinessDetails` on facade `capi`
- exposed `validateBusinessDetails` on facade `go`
- exposed `validateBusinessDetails` on facade `node-native`
- exposed `validateBusinessDetails` on facade `python`
- exposed `validateBusinessDetails` on facade `ruby`
- exposed `validateBusinessDetails` on facade `rust`
- exposed `validateBusinessDetails` on facade `typescript`
- exposed `validateBusinessDetails` on facade `wasm-browser`
- exposed `validateBusinessDetails` on facade `wasm-edge`
- exposed `validateCheckoutSessionParams` on facade `go`
- exposed `validateCheckoutSessionParams` on facade `node-native`
- exposed `validateCheckoutSessionParams` on facade `python`
- exposed `validateCheckoutSessionParams` on facade `ruby`
- exposed `validateCheckoutSessionParams` on facade `typescript`
- exposed `validateCheckoutSessionParams` on facade `wasm-edge`
- exposed `validateCreatePaymentIntentParams` on facade `go`
- exposed `validateCreatePaymentIntentParams` on facade `node-native`
- exposed `validateCreatePaymentIntentParams` on facade `python`
- exposed `validateCreatePaymentIntentParams` on facade `ruby`
- exposed `validateCreatePaymentIntentParams` on facade `typescript`
- exposed `validateCreatePaymentIntentParams` on facade `wasm-edge`
- exposed `validateGetProductParams` on facade `go`
- exposed `validateGetProductParams` on facade `node-native`
- exposed `validateGetProductParams` on facade `python`
- exposed `validateGetProductParams` on facade `ruby`
- exposed `validateGetProductParams` on facade `typescript`
- exposed `validateGetProductParams` on facade `wasm-edge`
- exposed `validateListPlansParams` on facade `go`
- exposed `validateListPlansParams` on facade `node-native`
- exposed `validateListPlansParams` on facade `python`
- exposed `validateListPlansParams` on facade `ruby`
- exposed `validateListPlansParams` on facade `typescript`
- exposed `validateListPlansParams` on facade `wasm-edge`
- exposed `validateProcessPaymentIntentParams` on facade `go`
- exposed `validateProcessPaymentIntentParams` on facade `node-native`
- exposed `validateProcessPaymentIntentParams` on facade `python`
- exposed `validateProcessPaymentIntentParams` on facade `ruby`
- exposed `validateProcessPaymentIntentParams` on facade `typescript`
- exposed `validateProcessPaymentIntentParams` on facade `wasm-edge`
- exposed `validatePublicBaseUrl` on facade `go`
- exposed `validatePublicBaseUrl` on facade `node-native`
- exposed `validatePublicBaseUrl` on facade `python`
- exposed `validatePublicBaseUrl` on facade `python-mcp`
- exposed `validatePublicBaseUrl` on facade `ruby`
- exposed `validatePublicBaseUrl` on facade `ruby-mcp`
- exposed `validatePublicBaseUrl` on facade `rust-mcp`
- exposed `validatePublicBaseUrl` on facade `typescript`
- exposed `validatePublicBaseUrl` on facade `wasm-edge`
- exposed `validatePurchaseRef` on facade `go`
- exposed `validatePurchaseRef` on facade `node-native`
- exposed `validatePurchaseRef` on facade `python`
- exposed `validatePurchaseRef` on facade `ruby`
- exposed `validatePurchaseRef` on facade `typescript`
- exposed `validatePurchaseRef` on facade `wasm-edge`
- exposed `validateTopupPaymentIntentParams` on facade `go`
- exposed `validateTopupPaymentIntentParams` on facade `node-native`
- exposed `validateTopupPaymentIntentParams` on facade `python`
- exposed `validateTopupPaymentIntentParams` on facade `ruby`
- exposed `validateTopupPaymentIntentParams` on facade `typescript`
- exposed `validateTopupPaymentIntentParams` on facade `wasm-edge`
- exposed `verifyWebhook` on facade `capi`
- exposed `verifyWebhook` on facade `go`
- exposed `verifyWebhook` on facade `node-native`
- exposed `verifyWebhook` on facade `python`
- exposed `verifyWebhook` on facade `ruby`
- exposed `verifyWebhook` on facade `rust`
- exposed `verifyWebhook` on facade `typescript`
- exposed `verifyWebhook` on facade `wasm-edge`
