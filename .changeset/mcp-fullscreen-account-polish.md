---
'@solvapay/react': minor
---

Drop the fullscreen `CloseButton` export — the host already owns window chrome. Portal "Full history / Full account" links render as text links, and the fullscreen shell centres at 1144px.
