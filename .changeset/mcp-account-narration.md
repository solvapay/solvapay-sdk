---
'@solvapay/mcp-core': patch
---

Account text-mode narration now branches on the nine v3 states (A–F, H–J) and reads `usage.total` / `remaining` / `periodEnd` instead of the unused `limit` / `resetsAt` fields.
