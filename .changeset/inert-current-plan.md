---
'@solvapay/react': patch
'@solvapay/release-train': patch
---

The customer's active plan is no longer selectable in the plan picker. The active PAYG plan is no longer auto-selected — topping up happens from the account surface, not by re-activating the current plan from the list.
