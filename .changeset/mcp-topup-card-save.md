---
'@solvapay/react': patch
---

Remove the decorative pre-checked save-card checkbox on PAYG checkout, and pass deferred auto-recharge settings into MCP top-up so the card is actually saved for off-session reuse.
