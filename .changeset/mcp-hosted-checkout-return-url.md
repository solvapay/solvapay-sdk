---
'@solvapay/mcp-core': patch
---

Hosted checkout sessions minted by MCP no longer send the synthetic server origin as `returnUrl`. The account link on the hosted page can point at the customer portal.
