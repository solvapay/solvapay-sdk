---
'@solvapay/server-wasm': minor
---

First public npm release of the edge/browser wasm-bindgen binding. `@solvapay/server` depends on this package and has no JavaScript fallback, so it must be on the registry before a server release can ship.
