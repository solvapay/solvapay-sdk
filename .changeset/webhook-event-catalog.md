---
"@solvapay/server": minor
---

Expand `WebhookEventType` to the full live backend event catalog and derive the public union from the generated schema with a `WebhookPayloadObject` fallback for unlisted types.
