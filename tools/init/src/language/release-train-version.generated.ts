/**
 * Offline scaffold pin stamped from `@solvapay/release-train`.
 *
 * Written by `tools/repo/sync-release-train.ts`. Not a second source of
 * truth — `pnpm checks:release-train` fails if this drifts from the sentinel.
 * `@solvapay/init` bundles this constant because the published CLI does not
 * ship the monorepo sentinel file.
 */
export const RELEASE_TRAIN_VERSION = '2.6.0'
