node_modules
dist
.cache
.next
.env
.env.*
!.env.example
!.env.example.*
!.env.platform-local.example
.npmrc
.pnpm-store
.turbo
.wrangler
.cursor/*
!.cursor/rules/
!.cursor/plans/
!.cursor/skills/
.DS_Store
*.tsbuildinfo
**/.demo-data/*
**/*.local.sh

# Deno — written by `deno check` during the CI validate gate. We pin
# workspace imports to `@preview` dist-tags on purpose (mutable; each
# gate run should pick up the latest preview), so committing a lockfile
# here would defeat the regression-detection window.
deno.lock

# Code coverage
coverage/
*.lcov
.nyc_output

# Generated documentation (TypeDoc output)
docs/api/
docs/api.json

# Scratch research notes — internal, not for the public repo
research-tmp/

# Contract / snapshot CLI test scratch
tools/.tmp/

# Shadow-mode live/selftest report output (step 25)
contract/shadow/output/

# Rust / Cargo build artifacts
target/
**/*.rs.bk
*.pdb

# napi-rs Node binding (step 36) — native/WASI binaries (loaders are committed)
sdks/node-native/*.node
sdks/node-native/*.wasm
sdks/node-native/node_modules/
sdks/node-native/npm/**/*.node
sdks/node-native/npm/**/*.wasm
sdks/node-native/npm/wasm32-wasi/server-native.wasi*
sdks/node-native/npm/wasm32-wasi/wasi-worker*
sdks/node-native/artifacts/
sdks/node-native/clean-install-bundle/
