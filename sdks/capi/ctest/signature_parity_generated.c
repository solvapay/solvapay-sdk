/* @generated by dto-gen (--c-parity-out) — do not edit. Regenerate with: pnpm gen */

#include "../include/solvapay.h"

#include <stdio.h>
#include <string.h>

/* Link-time refs: the cdylib must retain every public ABI symbol. */
static void keep_abi_symbols(void) {
  (void)&solvapay_abi_version;
  (void)&solvapay_version;
  (void)&solvapay_build_info;
  (void)&solvapay_client_new;
  (void)&solvapay_client_call;
  (void)&solvapay_verify_webhook;
  (void)&solvapay_client_free;
  (void)&solvapay_free_string;
}

static const char *kOps[] = {
  "createCustomer",
  "updateCustomer",
  "getCustomer",
  "assignCredits",
  "getCustomerBalance",
  "getUserInfo",
  "createCheckoutSession",
  "createCustomerSession",
  "getMerchant",
  "getPlatformConfig",
  "createPaymentIntent",
  "createTopupPaymentIntent",
  "processPaymentIntent",
  "attachBusinessDetails",
  "activatePlan",
  "checkLimits",
  "trackUsage",
  "trackUsageBulk",
  "getProduct",
  "listProducts",
  "createProduct",
  "updateProduct",
  "deleteProduct",
  "cloneProduct",
  "bootstrapMcpProduct",
  "configureMcpPlans",
  "listPlans",
  "createPlan",
  "updatePlan",
  "deletePlan",
  "cancelPurchase",
  "reactivatePurchase",
  "getPaymentMethod",
  "getAutoRecharge",
  "saveAutoRecharge",
  "disableAutoRecharge",
  "mcpBootstrap",
  "mcpCallBuiltinTool",
  "mcpReadResource",
  "mcpResolveAuth",
  "mcpOauthRequest",
  "mcpDispatch",
  "fetchJwks",
};

/* Required split-path args per op. The C envelope names the first missing key only,
* so the suite probes sequentially: fill prior keys, assert the next name appears.
*/
enum { kMaxRequired = 3 };
static const char *kRequiredArgs[][kMaxRequired] = {
  {NULL, NULL, NULL},
  {"customerRef", NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {"productRef", NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {"productRef", NULL, NULL},
  {"productRef", NULL, NULL},
  {"productRef", NULL, NULL},
  {NULL, NULL, NULL},
  {"productRef", NULL, NULL},
  {"productRef", NULL, NULL},
  {NULL, NULL, NULL},
  {"productRef", "planRef", NULL},
  {"productRef", "planRef", NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
  {NULL, NULL, NULL},
};

static int json_with_filled(char *buf, size_t bufsz, const char *const *args, size_t filled) {
	size_t n = 0;
	int wrote;
	wrote = snprintf(buf + n, bufsz - n, "{");
	if (wrote < 0 || (size_t)wrote >= bufsz - n) {
		return -1;
	}
	n += (size_t)wrote;
	for (size_t i = 0; i < filled; i++) {
		wrote = snprintf(buf + n, bufsz - n, "%s\"%s\":\"x\"", i == 0 ? "" : ",", args[i]);
		if (wrote < 0 || (size_t)wrote >= bufsz - n) {
			return -1;
		}
		n += (size_t)wrote;
	}
	wrote = snprintf(buf + n, bufsz - n, "}");
	if (wrote < 0 || (size_t)wrote >= bufsz - n) {
		return -1;
	}
	return 0;
}

int main(void) {
	keep_abi_symbols();
	if (solvapay_abi_version() != SOLVAPAY_ABI_VERSION) {
		fprintf(stderr, "FAIL: abi_version=%u header=%d\n",
		        solvapay_abi_version(), SOLVAPAY_ABI_VERSION);
		return 1;
	}
	size_t nops = sizeof(kOps) / sizeof(kOps[0]);
	if (nops != 43) {
		fprintf(stderr, "FAIL: kOps len = %zu, want 43\n", nops);
		return 1;
	}

	SolvapayClient *client = NULL;
	SolvapayStatus status = solvapay_client_new(
	    "{\"apiKey\":\"sk_parity\",\"apiBaseUrl\":\"http://127.0.0.1:1\"}",
	    &client);
	if (status != SolvapayStatus_Ok || client == NULL) {
		fprintf(stderr, "FAIL: client_new status=%d\n", (int)status);
		return 1;
	}
	for (size_t i = 0; i < nops; i++) {
		char *env = solvapay_client_call(client, kOps[i], "{}");
		if (env == NULL) {
			fprintf(stderr, "FAIL: %s returned null envelope\n", kOps[i]);
			solvapay_client_free(client);
			return 1;
		}
		if (strstr(env, "unknown op") != NULL) {
			fprintf(stderr, "FAIL: %s is unknown op: %s\n", kOps[i], env);
			solvapay_free_string(env);
			solvapay_client_free(client);
			return 1;
		}
		solvapay_free_string(env);
		for (size_t a = 0; a < (size_t)kMaxRequired && kRequiredArgs[i][a] != NULL; a++) {
			char json[256];
			if (json_with_filled(json, sizeof(json), kRequiredArgs[i], a) != 0) {
				fprintf(stderr, "FAIL: %s json buffer overflow\n", kOps[i]);
				solvapay_client_free(client);
				return 1;
			}
			env = solvapay_client_call(client, kOps[i], json);
			if (env == NULL) {
				fprintf(stderr, "FAIL: %s returned null envelope for %s\n", kOps[i], json);
				solvapay_client_free(client);
				return 1;
			}
			if (strstr(env, kRequiredArgs[i][a]) == NULL) {
				fprintf(stderr, "FAIL: %s missing %s in %s: %s\n",
				        kOps[i], kRequiredArgs[i][a], json, env);
				solvapay_free_string(env);
				solvapay_client_free(client);
				return 1;
			}
			solvapay_free_string(env);
		}
	}
	solvapay_client_free(client);
	printf("OK: C signature parity (43 ops)\n");
	return 0;
}
