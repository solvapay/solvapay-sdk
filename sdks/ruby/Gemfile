# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Step 45T — dynamic-language strictness gate (D18). Pinned exact versions.
group :typecheck do
  gem "rbs", "4.0.3"
  gem "steep", "2.0.0"
  gem "rubocop", "1.88.2"
end
