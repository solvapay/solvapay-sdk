# @generated by dto-gen (--rb-drivers-out) — do not edit. Regenerate with: pnpm gen

# frozen_string_literal: true

module SolvaPay
  module GeneratedGateLoop
    def self.run(gate_next:, host:, start_event:)
      state = nil
      event = start_event
      loop do
        out = gate_next.call(state, event)
        unless out.is_a?(Hash)
          raise SolvaPay::SolvaPayError.new("gate_next returned unexpected value", code: "internal_error")
        end

        state = out["state"]
        action = out["action"]
        unless action.is_a?(Hash)
          raise SolvaPay::SolvaPayError.new("gate_next returned unexpected action", code: "internal_error")
        end

        case action["kind"]
        when "ensureCustomer"
          backend_ref = host.ensure_customer(action["customerRef"])
          event = { "kind" => "customerResolved", "backendRef" => backend_ref, "nowMs" => host.now_ms }
        when "readLimitsCache"
          cached = host.read_limits_cache(action["key"])
          now = host.now_ms
          event = if cached.is_a?(Hash)
                    {
                      "kind" => "limitsCacheEntry",
                      "found" => true,
                      "remaining" => cached.fetch(:remaining),
                      "limits" => cached[:limits],
                      "timestampMs" => cached.fetch(:timestamp),
                      "nowMs" => now,
                    }
                  else
                    { "kind" => "limitsCacheEntry", "found" => false, "nowMs" => now }
                  end
        when "checkLimits"
          limits = host.check_limits(action)
          event = { "kind" => "limitsResult", "limits" => limits, "nowMs" => host.now_ms }
        when "allow", "gate"
          host.apply_cache(action["cache"])
          return { "state" => state, "action" => action }
        when "emitUsage", "skipUsage"
          kind = action["kind"]
          raise SolvaPay::SolvaPayError.new(
            "gate_next returned #{kind} during decide",
            code: "internal_error",
          )
        else
          raise SolvaPay::SolvaPayError.new("gate_next returned unknown action kind", code: "internal_error")
        end
      end
    end
  end

  module GeneratedPayableLoop
    def self.run(payable_next:, host:, start_event:)
      state = nil
      event = start_event
      loop do
        out = payable_next.call(state, event)
        unless out.is_a?(Hash)
          raise SolvaPay::SolvaPayError.new(
            "invoke_payable_next returned unexpected value",
            code: "internal_error",
          )
        end

        state = out["state"]
        action = out["action"]
        unless action.is_a?(Hash)
          raise SolvaPay::SolvaPayError.new(
            "invoke_payable_next returned unexpected action",
            code: "internal_error",
          )
        end

        case action["kind"]
        when "runGate"
          gate = host.run_gate(action)
          return gate[:result] if gate[:kind] == :return || gate["kind"] == "return"

          kind = gate[:kind] || gate["kind"]
          event = if [:paywall, "paywall"].include?(kind)
                    {
                      "kind" => "gatePaywall",
                      "gate" => gate[:gate] || gate["gate"],
                      "message" => gate[:message] || gate["message"],
                    }
                  else
                    {
                      "kind" => "gateAllow",
                      "customerRef" => gate[:customerRef] || gate["customerRef"],
                      "limits" => gate[:limits] || gate["limits"],
                    }
                  end
        when "invokeHandler"
          invoked = host.invoke_handler(action)
          return invoked[:result] if invoked[:kind] == :return || invoked["kind"] == "return"

          kind = invoked[:kind] || invoked["kind"]
          event = if [:paywall, "paywall"].include?(kind)
                    {
                      "kind" => "handlerPaywall",
                      "gate" => invoked[:gate] || invoked["gate"],
                      "message" => invoked[:message] || invoked["message"],
                    }
                  elsif [:err, "err"].include?(kind)
                    {
                      "kind" => "handlerErr",
                      "message" => invoked[:message] || invoked["message"],
                      "nowMs" => host.now_ms,
                      "randomUnit" => host.random_unit,
                    }
                  else
                    {
                      "kind" => "handlerOk",
                      "envelope" => invoked[:envelope] || invoked["envelope"],
                      "nowMs" => host.now_ms,
                      "randomUnit" => host.random_unit,
                    }
                  end
        when "done"
          track = action["track"]
          host.track_usage(track["request"]) if track.is_a?(Hash) && !track["request"].nil?
          return action["result"]
        else
          kind = action["kind"]
          raise SolvaPay::SolvaPayError.new(
            "invoke_payable_next unknown action kind: #{kind}",
            code: "internal_error",
          )
        end
      end
    end
  end
end
