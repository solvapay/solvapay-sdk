/**
 * Limits helper types (Step 52). Helpers are Rust-only in `native-helpers.ts`.
 */

export type {
  CheckLimitsParams,
  FreeLimit,
  FreeLimitInput,
  FreeLimitScope,
  LimitsHelperError,
} from './types/boundary.generated'
