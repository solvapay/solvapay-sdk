/**
 * @generated by dto-gen (--ts-envelope-out) — do not edit. Regenerate with: pnpm gen
 */

import {
  reconstructSolvaPayEnvelopeError,
  unwrapEnvelope as unwrapCoreEnvelope,
  type EnvelopeError,
} from '@solvapay/core'
import { PaywallError } from './paywall'
import type { PaywallStructuredContent } from './types/paywall'

export type { EnvelopeError }

/** Maps a JSON envelope error to the frozen TypeScript error classes. */
export function reconstructEnvelopeError(error: EnvelopeError): Error {
  if (error.kind === 'Paywall') {
    return new PaywallError(error.message, error.gate as PaywallStructuredContent)
  }
  return reconstructSolvaPayEnvelopeError(error)
}

/** Parses a JSON envelope string and returns `value` or throws reconstructed errors. */
export function unwrapEnvelope(envelopeJson: string, source: string): unknown {
  return unwrapCoreEnvelope(envelopeJson, source, reconstructEnvelopeError)
}
