# @generated by dto-gen — do not edit. Regenerate with --py-stub-out.

"""Portable Python surface stubs (generated from the SDK contract IR)."""

from __future__ import annotations

from solvapay.errors import PaywallError as PaywallError
from solvapay.facade import (
    ApiClient as ApiClient,
    SolvaPay as SolvaPay,
    create_solvapay as create_solvapay,
)
from solvapay.results import (
    PayableAllowResult as PayableAllowResult,
    PayableGateResult as PayableGateResult,
    PayablePaywallResult as PayablePaywallResult,
)

class SolvaPayError(Exception):
    code: str
    status: int | None

class SolvaPayClient:
    def __init__(self, api_key: str, api_base_url: str | None = None) -> None: ...
    @staticmethod
    def _for_fixtures(
        api_key: str,
        api_base_url: str,
        clock_unix_ms: int | None = None,
        rng_seed: int | None = None,
    ) -> SolvaPayClient: ...
    async def activate_plan(self, args_json: str) -> str:
        """Activate a plan for a customer (purchase or entitlement grant).
        @param params Plan activation request fields.
        @returns Activation result projection.
        """
        ...
    def activate_plan_blocking(self, args_json: str) -> str:
        """Activate a plan for a customer (purchase or entitlement grant).
        @param params Plan activation request fields.
        @returns Activation result projection.
        """
        ...
    async def assign_credits(self, args_json: str) -> str:
        """Grant credits to a customer balance.
        @param params Credit grant request (customer, amount, and reason).
        @returns Credit assignment response.
        """
        ...
    def assign_credits_blocking(self, args_json: str) -> str:
        """Grant credits to a customer balance.
        @param params Credit grant request (customer, amount, and reason).
        @returns Credit assignment response.
        """
        ...
    async def attach_business_details(self, args_json: str) -> str:
        """Attach or update business details used for tax and invoicing.
        @param params Business details payload.
        @returns Attached business details result.
        """
        ...
    def attach_business_details_blocking(self, args_json: str) -> str:
        """Attach or update business details used for tax and invoicing.
        @param params Business details payload.
        @returns Attached business details result.
        """
        ...
    async def bootstrap_mcp_product(self, args_json: str) -> str:
        """Bootstrap an MCP product with default plans and tooling.
        @param params MCP bootstrap request payload.
        @returns Bootstrap result including product and plan refs.
        """
        ...
    def bootstrap_mcp_product_blocking(self, args_json: str) -> str:
        """Bootstrap an MCP product with default plans and tooling.
        @param params MCP bootstrap request payload.
        @returns Bootstrap result including product and plan refs.
        """
        ...
    async def cancel_purchase(self, args_json: str) -> str:
        """Cancel an active purchase for a customer.
        @param params Cancel request identifying the purchase.
        @returns Updated purchase info after cancellation.
        """
        ...
    def cancel_purchase_blocking(self, args_json: str) -> str:
        """Cancel an active purchase for a customer.
        @param params Cancel request identifying the purchase.
        @returns Updated purchase info after cancellation.
        """
        ...
    async def check_limits(self, args_json: str) -> str:
        """Check remaining usage/spend limits for a customer against a product's plan.
        @param params Limits request including customer and product refs.
        @returns Current remaining limits, optionally including plan details.
        """
        ...
    def check_limits_blocking(self, args_json: str) -> str:
        """Check remaining usage/spend limits for a customer against a product's plan.
        @param params Limits request including customer and product refs.
        @returns Current remaining limits, optionally including plan details.
        """
        ...
    async def clone_product(self, args_json: str) -> str:
        """Clone a product, optionally applying field overrides.
        @param overrides Optional field overrides applied to the clone.
        @returns The cloned product projection.
        """
        ...
    def clone_product_blocking(self, args_json: str) -> str:
        """Clone a product, optionally applying field overrides.
        @param overrides Optional field overrides applied to the clone.
        @returns The cloned product projection.
        """
        ...
    async def configure_mcp_plans(self, args_json: str) -> str:
        """Configure MCP plans for an existing product.
        @param params MCP plan configuration payload.
        @returns Configured MCP plans response.
        """
        ...
    def configure_mcp_plans_blocking(self, args_json: str) -> str:
        """Configure MCP plans for an existing product.
        @param params MCP plan configuration payload.
        @returns Configured MCP plans response.
        """
        ...
    async def create_checkout_session(self, args_json: str) -> str:
        """Create a hosted checkout session for a customer and product.
        @param params Checkout session creation fields.
        @returns Created checkout session projection.
        """
        ...
    def create_checkout_session_blocking(self, args_json: str) -> str:
        """Create a hosted checkout session for a customer and product.
        @param params Checkout session creation fields.
        @returns Created checkout session projection.
        """
        ...
    async def create_customer(self, args_json: str) -> str:
        """Create a customer in SolvaPay for the current merchant.
        @param params Customer creation fields (email, external refs, metadata).
        @returns The created customer projection.
        """
        ...
    def create_customer_blocking(self, args_json: str) -> str:
        """Create a customer in SolvaPay for the current merchant.
        @param params Customer creation fields (email, external refs, metadata).
        @returns The created customer projection.
        """
        ...
    async def create_customer_session(self, args_json: str) -> str:
        """Create a customer portal/session for self-serve account actions.
        @param params Customer session creation fields.
        @returns Created customer session projection.
        """
        ...
    def create_customer_session_blocking(self, args_json: str) -> str:
        """Create a customer portal/session for self-serve account actions.
        @param params Customer session creation fields.
        @returns Created customer session projection.
        """
        ...
    async def create_payment_intent(self, args_json: str) -> str:
        """Create a payment intent for a purchase or activation flow.
        @param params Payment intent creation fields.
        @returns Created payment intent projection.
        """
        ...
    def create_payment_intent_blocking(self, args_json: str) -> str:
        """Create a payment intent for a purchase or activation flow.
        @param params Payment intent creation fields.
        @returns Created payment intent projection.
        """
        ...
    async def create_plan(self, args_json: str) -> str:
        """Create a plan under a product.
        @param params Plan creation fields including product ref.
        @returns The created plan projection.
        """
        ...
    def create_plan_blocking(self, args_json: str) -> str:
        """Create a plan under a product.
        @param params Plan creation fields including product ref.
        @returns The created plan projection.
        """
        ...
    async def create_product(self, args_json: str) -> str:
        """Create a product for the current merchant.
        @param params Product creation fields.
        @returns The created product projection.
        """
        ...
    def create_product_blocking(self, args_json: str) -> str:
        """Create a product for the current merchant.
        @param params Product creation fields.
        @returns The created product projection.
        """
        ...
    async def create_topup_payment_intent(self, args_json: str) -> str:
        """Create a payment intent to top up a customer balance.
        @param params Top-up payment intent fields.
        @returns Created top-up payment intent projection.
        """
        ...
    def create_topup_payment_intent_blocking(self, args_json: str) -> str:
        """Create a payment intent to top up a customer balance.
        @param params Top-up payment intent fields.
        @returns Created top-up payment intent projection.
        """
        ...
    async def delete_plan(self, args_json: str) -> str:
        """Delete a plan by product and plan references."""
        ...
    def delete_plan_blocking(self, args_json: str) -> str:
        """Delete a plan by product and plan references."""
        ...
    async def delete_product(self, args_json: str) -> str:
        """Delete a product by reference."""
        ...
    def delete_product_blocking(self, args_json: str) -> str:
        """Delete a product by reference."""
        ...
    async def disable_auto_recharge(self, args_json: str) -> str:
        """Disable auto-recharge for a customer.
        @param params Disable request identifying the customer.
        @returns Updated auto-recharge status after disable.
        """
        ...
    def disable_auto_recharge_blocking(self, args_json: str) -> str:
        """Disable auto-recharge for a customer.
        @param params Disable request identifying the customer.
        @returns Updated auto-recharge status after disable.
        """
        ...
    async def get_auto_recharge(self, args_json: str) -> str:
        """Fetch auto-recharge configuration for a customer.
        @param params Auto-recharge lookup options.
        @returns Auto-recharge configuration projection.
        """
        ...
    def get_auto_recharge_blocking(self, args_json: str) -> str:
        """Fetch auto-recharge configuration for a customer.
        @param params Auto-recharge lookup options.
        @returns Auto-recharge configuration projection.
        """
        ...
    async def get_customer(self, args_json: str) -> str:
        """Fetch a customer by reference.
        @param params Lookup options including the customer reference.
        @returns The customer projection.
        """
        ...
    def get_customer_blocking(self, args_json: str) -> str:
        """Fetch a customer by reference.
        @param params Lookup options including the customer reference.
        @returns The customer projection.
        """
        ...
    async def get_customer_balance(self, args_json: str) -> str:
        """Fetch a customer credit balance projection for display.
        @param params Balance request identifying the customer.
        @returns Customer balance display projection.
        """
        ...
    def get_customer_balance_blocking(self, args_json: str) -> str:
        """Fetch a customer credit balance projection for display.
        @param params Balance request identifying the customer.
        @returns Customer balance display projection.
        """
        ...
    async def get_merchant(self, args_json: str) -> str:
        """Fetch the authenticated merchant profile.
        @returns Merchant profile projection.
        """
        ...
    def get_merchant_blocking(self, args_json: str) -> str:
        """Fetch the authenticated merchant profile.
        @returns Merchant profile projection.
        """
        ...
    async def get_payment_method(self, args_json: str) -> str:
        """Fetch the default payment method for a customer.
        @param params Payment-method lookup options.
        @returns Payment method info, when present.
        """
        ...
    def get_payment_method_blocking(self, args_json: str) -> str:
        """Fetch the default payment method for a customer.
        @param params Payment-method lookup options.
        @returns Payment method info, when present.
        """
        ...
    async def get_platform_config(self, args_json: str) -> str:
        """Fetch platform configuration for the current merchant.
        @returns Platform configuration projection.
        """
        ...
    def get_platform_config_blocking(self, args_json: str) -> str:
        """Fetch platform configuration for the current merchant.
        @returns Platform configuration projection.
        """
        ...
    async def get_product(self, args_json: str) -> str:
        """Fetch a product by reference.
        @returns Product projection.
        """
        ...
    def get_product_blocking(self, args_json: str) -> str:
        """Fetch a product by reference.
        @returns Product projection.
        """
        ...
    async def get_user_info(self, args_json: str) -> str:
        """Fetch end-user info for a customer session.
        @param params User-info request options.
        @returns User info projection.
        """
        ...
    def get_user_info_blocking(self, args_json: str) -> str:
        """Fetch end-user info for a customer session.
        @param params User-info request options.
        @returns User info projection.
        """
        ...
    async def list_plans(self, args_json: str) -> str:
        """List plans for a product.
        @returns Plan list projection.
        """
        ...
    def list_plans_blocking(self, args_json: str) -> str:
        """List plans for a product.
        @returns Plan list projection.
        """
        ...
    async def list_products(self, args_json: str) -> str:
        """List products for the current merchant.
        @returns Product list projection.
        """
        ...
    def list_products_blocking(self, args_json: str) -> str:
        """List products for the current merchant.
        @returns Product list projection.
        """
        ...
    async def process_payment_intent(self, args_json: str) -> str:
        """Process a completed payment intent into a purchase or top-up outcome.
        @param params Process request identifying the payment intent.
        @returns Normalized process-payment result (purchase, top-up, or error branch).
        """
        ...
    def process_payment_intent_blocking(self, args_json: str) -> str:
        """Process a completed payment intent into a purchase or top-up outcome.
        @param params Process request identifying the payment intent.
        @returns Normalized process-payment result (purchase, top-up, or error branch).
        """
        ...
    async def reactivate_purchase(self, args_json: str) -> str:
        """Reactivate a previously cancelled purchase.
        @param params Reactivate request identifying the purchase.
        @returns Updated purchase info after reactivation.
        """
        ...
    def reactivate_purchase_blocking(self, args_json: str) -> str:
        """Reactivate a previously cancelled purchase.
        @param params Reactivate request identifying the purchase.
        @returns Updated purchase info after reactivation.
        """
        ...
    async def save_auto_recharge(self, args_json: str) -> str:
        """Create or update auto-recharge configuration for a customer.
        @param params Auto-recharge settings to persist.
        @returns Saved auto-recharge configuration.
        """
        ...
    def save_auto_recharge_blocking(self, args_json: str) -> str:
        """Create or update auto-recharge configuration for a customer.
        @param params Auto-recharge settings to persist.
        @returns Saved auto-recharge configuration.
        """
        ...
    async def track_usage(self, args_json: str) -> str:
        """Record a single usage event against a meter for billing.
        @param params Usage event payload (customer, meter, and amount).
        @returns The recorded usage event response.
        """
        ...
    def track_usage_blocking(self, args_json: str) -> str:
        """Record a single usage event against a meter for billing.
        @param params Usage event payload (customer, meter, and amount).
        @returns The recorded usage event response.
        """
        ...
    async def track_usage_bulk(self, args_json: str) -> str:
        """Record multiple usage events in one request.
        @param params Bulk usage payload with one or more events.
        @returns Bulk usage recording response.
        """
        ...
    def track_usage_bulk_blocking(self, args_json: str) -> str:
        """Record multiple usage events in one request.
        @param params Bulk usage payload with one or more events.
        @returns Bulk usage recording response.
        """
        ...
    async def update_customer(self, args_json: str) -> str:
        """Update an existing customer by reference.
        @param params Fields to patch on the customer.
        @returns The updated customer projection.
        """
        ...
    def update_customer_blocking(self, args_json: str) -> str:
        """Update an existing customer by reference.
        @param params Fields to patch on the customer.
        @returns The updated customer projection.
        """
        ...
    async def update_plan(self, args_json: str) -> str:
        """Update an existing plan by product and plan references.
        @param params Fields to patch on the plan.
        @returns The updated plan projection.
        """
        ...
    def update_plan_blocking(self, args_json: str) -> str:
        """Update an existing plan by product and plan references.
        @param params Fields to patch on the plan.
        @returns The updated plan projection.
        """
        ...
    async def update_product(self, args_json: str) -> str:
        """Update an existing product by reference.
        @param params Fields to patch on the product.
        @returns The updated product projection.
        """
        ...
    def update_product_blocking(self, args_json: str) -> str:
        """Update an existing product by reference.
        @param params Fields to patch on the product.
        @returns The updated product projection.
        """
        ...

def version() -> str:
    """Return the installed solvapay package / native module version."""
    ...
def native_build_info() -> str:
    """Return `{version, coreSha}` JSON for version-stamping diagnostics."""
    ...
def verify_webhook(body: str, signature: str, secret: str) -> str:
    """Verify a SolvaPay webhook signature and parse the event payload.
    @param options Webhook body, signature header, and signing secret.
    @returns Parsed webhook event when verification succeeds.
    """
    ...
def _verify_webhook_at(
    body: str, signature: str, secret: str, now_unix_secs: int
) -> str: ...
