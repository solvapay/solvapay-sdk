/**
 * wasm2js browser profile — public-safe pure logic, no WebAssembly.
 *
 * Same explicit symbol list as `runtime/browser-web.js`. There is no
 * `ready` / `readyFromBytes` / `ensureReadySync`: the converted module
 * is live as soon as this file is imported. The marker string must stay
 * a runtime value so the MCP App widget artifact gate can see it.
 */
import {
  wasmVersion,
  wasmBuildInfo,
  validateBusinessDetails,
  deriveTaxIdType,
  isTaxIdType,
  TAX_ID_TYPES,
  resolveTaxBehavior,
  getTaxIdExample,
  getTaxIdFieldLabel,
  getTaxIdHelperText,
  getBusinessCountryOptions,
  resolveBuyerCountry,
  getCustomerAddressFieldErrors,
  isCustomerAddressComplete,
  isPostalCodeRequired,
  isStateRequired,
  getStateFieldLabel,
  getPostalCodeFieldLabel,
  getPostalCodePlaceholder,
  POSTAL_CODE_REQUIRED_COUNTRIES,
  STATE_REQUIRED_COUNTRIES,
  BUSINESS_COUNTRY_DISPLAY_NAMES,
  BUSINESS_COUNTRY_OPTIONS,
  SUPPORTED_BUSINESS_COUNTRIES,
  COUNTRY_TO_TAX_ID_TYPE,
  TAX_ID_EXAMPLE_BY_COUNTRY,
  TAX_BEHAVIORS,
  TAX_EXCLUSIVE_CURRENCIES,
  creditsToDisplayMinorUnits,
  isZeroDecimalCurrency,
  isUnlimitedRemaining,
  minorUnitsPerMajor,
  resolveSellerIdentityDisplay,
  getSellerTaxIdentifierDisplayLabel,
  SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE,
  solvapayTermsUrl,
  solvapayPrivacyUrl,
  solvapayWebsiteUrl,
  resolveMandateLegalDocs,
  charges,
  headlineCharges,
  perUnitCharge,
  billingCycle,
  trialDays,
  includedUnits,
  countsUsage,
  meterName,
  usageRate,
  peggedCreditsPerUnit,
  creditsPerUnitFromBalance,
  planPricingShape,
  resolveAccountState,
  deriveDefaultView,
  resolvePlanShape,
  planConsequence,
  deriveActiveProducts,
  historyRows,
  formatCompactCredits,
  resolveDisplayMode,
  formatPrice,
  formatSubtotalLabel,
  formatVatSummaryLabel,
  resolveTaxTreatmentNote,
  shouldShowTaxRow,
  toMajorUnits,
  TOPUP_BALANCE_POLL_DELAYS_MS,
  BALANCE_RECONCILE_DELAYS_MS,
  REVERSE_CHARGE_NOTE,
  TAX_NOT_COLLECTED_NOTE,
  assertValidProductRef,
  attachBusinessDetailsValidationError,
  classifyCancelError,
  classifyCreateError,
  classifyLookupError,
  classifyReactivateError,
  freeMeterNamePattern,
  isCachedCustomerRefValid,
  isEmailConflict,
  normalizeCancelResponse,
  normalizeReactivateResponse,
  requireProductRef,
  resolveProductRef,
  validateAttachBusinessDetailsParams,
  validateCheckoutSessionParams,
  validateCreatePaymentIntentParams,
  validateGetProductParams,
  validateListPlansParams,
  validateProcessPaymentIntentParams,
  validatePurchaseRef,
} from '../pkg/browser-js/solvapay_wasm.js'

export const SOLVAPAY_BROWSER_JS_CORE = 'solvapay-browser-js-core'

export {
  wasmVersion,
  wasmBuildInfo,
  validateBusinessDetails,
  deriveTaxIdType,
  isTaxIdType,
  TAX_ID_TYPES,
  resolveTaxBehavior,
  getTaxIdExample,
  getTaxIdFieldLabel,
  getTaxIdHelperText,
  getBusinessCountryOptions,
  resolveBuyerCountry,
  getCustomerAddressFieldErrors,
  isCustomerAddressComplete,
  isPostalCodeRequired,
  isStateRequired,
  getStateFieldLabel,
  getPostalCodeFieldLabel,
  getPostalCodePlaceholder,
  POSTAL_CODE_REQUIRED_COUNTRIES,
  STATE_REQUIRED_COUNTRIES,
  BUSINESS_COUNTRY_DISPLAY_NAMES,
  BUSINESS_COUNTRY_OPTIONS,
  SUPPORTED_BUSINESS_COUNTRIES,
  COUNTRY_TO_TAX_ID_TYPE,
  TAX_ID_EXAMPLE_BY_COUNTRY,
  TAX_BEHAVIORS,
  TAX_EXCLUSIVE_CURRENCIES,
  creditsToDisplayMinorUnits,
  isZeroDecimalCurrency,
  isUnlimitedRemaining,
  minorUnitsPerMajor,
  resolveSellerIdentityDisplay,
  getSellerTaxIdentifierDisplayLabel,
  SELLER_TAX_IDENTIFIER_DISPLAY_LABEL_BY_TYPE,
  solvapayTermsUrl,
  solvapayPrivacyUrl,
  solvapayWebsiteUrl,
  resolveMandateLegalDocs,
  charges,
  headlineCharges,
  perUnitCharge,
  billingCycle,
  trialDays,
  includedUnits,
  countsUsage,
  meterName,
  usageRate,
  peggedCreditsPerUnit,
  creditsPerUnitFromBalance,
  planPricingShape,
  resolveAccountState,
  deriveDefaultView,
  resolvePlanShape,
  planConsequence,
  deriveActiveProducts,
  historyRows,
  formatCompactCredits,
  resolveDisplayMode,
  formatPrice,
  formatSubtotalLabel,
  formatVatSummaryLabel,
  resolveTaxTreatmentNote,
  shouldShowTaxRow,
  toMajorUnits,
  TOPUP_BALANCE_POLL_DELAYS_MS,
  BALANCE_RECONCILE_DELAYS_MS,
  REVERSE_CHARGE_NOTE,
  TAX_NOT_COLLECTED_NOTE,
  assertValidProductRef,
  attachBusinessDetailsValidationError,
  classifyCancelError,
  classifyCreateError,
  classifyLookupError,
  classifyReactivateError,
  freeMeterNamePattern,
  isCachedCustomerRefValid,
  isEmailConflict,
  normalizeCancelResponse,
  normalizeReactivateResponse,
  requireProductRef,
  resolveProductRef,
  validateAttachBusinessDetailsParams,
  validateCheckoutSessionParams,
  validateCreatePaymentIntentParams,
  validateGetProductParams,
  validateListPlansParams,
  validateProcessPaymentIntentParams,
  validatePurchaseRef,
}
